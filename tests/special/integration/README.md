# Integration coverage

`tests/special/test_marketdata_public.py` performs a fixture-backed collect, append,
readback, identity, missing-endpoint, and receipt integration test. Actual unsigned
collection evidence is kept outside the repository.
