# Trading Room 작업 규약

- 먼저 `README.md`, `config/tracks.json`, 해당 `track_a|b|c/README.md`를 읽는다. 문서의 운영 상태는 `python -m track_c.ops.status`로 실제 배포와 대조한다.
- A/B는 정지 상태다. 감독·감시견·Monitor를 자동 재개하지 않는다. C는 AWS의 고정 릴리스에서 기본 규칙으로 BTC 실체결을 수집한다.
- 수정 요청은 코드·테스트 작업을 허용한다. 정리·테스트만을 이유로 실거래를 재배포하거나 재기동하지 않는다. 감독 세션이 있으면 프로세스와 운영 설정은 그 세션이 소유한다.
- 전략의 계약은 트랙별 README, 숫자는 설정, 구현은 코드로 확인한다. A는 순환매, B는 독립 캠페인이며 서로의 전략 규칙을 상속하지 않는다.
- 실제 결함만 최소 변경하고 `python -m unittest discover -s tests -t .`를 실행한다. A/B 전략을 바꾸면 보존된 기준 테이프의 `track_a.backtest`도 대조한다.
- `.env`는 읽기만 하고 출력·전송하지 않는다. 사용자 포지션·주문·계좌 자금을 임의로 조작하지 않는다. 엔진 주문과 거래소 보호 주문의 소유권·복구·부분체결 대사를 보존한다.
- C의 원본 `track_c/research.md`, 고정 모델, 평가 등록, 장부, 표시 기준선을 덮어쓰지 않는다. 변경 소스·모델·평가는 별도 식별자와 출력 경로를 사용한다. 자동 재학습·증액·실거래 승격을 추가하지 않는다.
- 실체결 자료 수집은 승인된 기본 규칙을 유지한다. 거래를 늘리려고 임계값·종목·현금·위험 한도를 임의로 완화하지 않는다.
- Slack은 기존 릴레이 하나만 사용한다. 알림 수정으로 매매 엔진을 재기동하지 않는다. C 원화 장부와 A/B USDT 장부를 섞지 않는다. 기존 BTC·진입 편차·표시 창 필터와 원화 소수점 첫째 자리 표시를 유지한다.
- 코드와 회귀 테스트는 레포에, 녹화·장부·실험 출력은 `TRADING_ROOM_HOME` 또는 형제 디렉터리 `trading-room-state/`에 둔다. 일회성 스크립트는 OS 임시 디렉터리에서 사용한다. 날짜별 문서와 일회성 배포 스크립트를 레포에 쌓지 않는다.
- Windows의 삭제·이동은 같은 명령 안에서 절대 경로와 대상 범위를 확인한다. flat 확인과 프로세스 종료를 분리하지 않는다. 백그라운드 프로세스는 `Start-Process -WindowStyle Hidden`을 사용한다.
- 데스크톱 UI 조작은 공식 OpenAI Computer Use 플러그인을 사용한다. 먼저 `C:/Users/dongh/.codex/skills/openai-computer-use/SKILL.md`를 읽는다. Orca Computer Use로 대체하지 않는다. Orca CLI의 터미널·워크트리·내장 브라우저 사용은 별개다.
