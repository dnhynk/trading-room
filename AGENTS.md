# Trading Room 작업 규약

- 먼저 `README.md`, `config/tracks.json`, 해당 트랙의 README를 읽는다. 공개 설정은 예시이며 실제 운영 상태를 가정하지 않는다.
- 전략의 계약은 트랙별 README, 숫자는 설정, 구현은 코드로 확인한다. A는 순환매, B는 독립 캠페인이며 서로의 전략 규칙을 상속하지 않는다.
- 기본값은 A/B 정지·dry, C observe다. 코드 수정·정리·테스트만을 이유로 감독 프로세스나 실거래를 시작·재배포·재기동하지 않는다.
- 실제 결함만 최소 변경하고 `python -m unittest discover -s tests -t .`를 실행한다. 기준 테이프를 보유한 경우 A/B 전략 변경은 동일 입력의 `track_a.backtest`와도 대조한다.
- `.env`·키·서버 식별자·실계좌 자료를 출력·전송·커밋하지 않는다. 사용자 포지션·주문·계좌 자금을 임의로 조작하지 않는다. 주문 소유권·보호 주문·복구·부분체결 대사를 보존한다.
- C의 `track_c/research.md`, 고정 모델, 평가 등록, 장부, 표시 기준선을 덮어쓰지 않는다. 변경 모델·평가는 별도 식별자와 출력 경로를 사용한다.
- 자동 재학습·증액·실거래 전환을 추가하지 않는다. 거래를 늘리려고 임계값·종목·현금·위험 한도를 임의로 완화하지 않는다.
- 공개 재생 결과를 실체결 검증이나 손익 하한으로 부르지 않는다. 관측 중단·부분체결·잔량과 같은 에피소드의 종속 표본을 보존한다.
- 알림 릴레이는 운영자가 명시적으로 구성한다. C 원화 장부와 A/B USDT 장부를 섞지 않으며 원화 표시는 소수점 첫째 자리, 내부 계산은 원래 정밀도를 유지한다.
- 녹화·장부·실험 출력은 외부 상태 디렉터리, 일회성 스크립트는 OS 임시 디렉터리에 둔다. 날짜별 문서와 일회성 배포 스크립트를 쌓지 않는다.
- Windows의 삭제·이동은 같은 명령 안에서 절대 경로와 대상 범위를 확인한다. flat 확인과 프로세스 종료를 분리하지 않는다. 백그라운드 프로세스는 `Start-Process -WindowStyle Hidden`을 사용한다.
