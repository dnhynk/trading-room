# 순환매 스크립트 규칙 (현재 계약)

상위 계약은 `bot/CONCEPT.md`(자연어 전략)이고, 이 문서는 `bot/cycle.py`가 그것을 지금 어떻게 구현하는지의 계약이다. 매매 세션에서 규칙이 바뀌면 이 문서를 먼저 고치고 스크립트를 맞춘다. 숫자는 `params.json`이 진실이고, 여기 적힌 값은 기본값이다. 메커니즘은 사용자가 자연어로 정한 것이고 숫자는 전부 휴리스틱이다(근거 있는 건 수수료 바닥 왕복 0.04%뿐).

## 신호 (`bot/signal.py` Features, 방향 무관)
- **DIP_SLOWING / POP_STALLING** — 두 규칙 중 하나면 발화(`src`로 구분, 재생에서 성적 비교). 세 번째 규칙 `s8`은 기록 전용이다(아래).
  - `v`: 초 단위 속도 모델. 최근 10분 극값에서 `dip_min_atr`(3 ATR) 이상 움직였고, 그 다리에서 속도 `|v| ≥ v_fast`(1.0σ)였다가 지금 `|v| ≤ v_slow`(0.3σ)로 죽고 가속이 반대로 3초 연속. σ(1초 수익률 RMS, 반감기 `vol_hl` 300초)와 속도 EMA는 창이 찰 때까지 확장 평균으로 시작하고, **σ에 반감기만큼(300초)의 데이터가 쌓이기 전엔 `v` 규칙이 침묵한다**(시작·재기동·120초 이상 끊김 뒤 — 끊김은 σ도 같이 리셋한다) — 첫 샘플에서 v가 정확히 ±1이 되어 fast 조건을 통과하던 결함: 2026-08-29 live `v` 신호 11개 중 8개가 START 3분 이내(21:25:43 진입은 재기동 18초 뒤). 그동안은 `1m` 규칙만 발화한다. **5초를 넘는 피드 갭의 첫 수익률은 σ에도 속도에도 넣지 않는다** — 갭 전체의 이동이 1초 수익률로 들어가면 σ가 부풀고 v가 튀어 가짜 fast→slow 전이가 된다.
  - `1m`: 매매 세션 감시자(watch.py v13) 규칙 + 2026-08-29 교훈. 30봉 극값 대비 0.4% 이상 눌림/반등에서, (a) **클라이맥스 소멸**: 직전 두 봉 중 거래량 1.5배 이상의 봉이 있고 지금 봉 거래량이 직전 봉보다 줄었으며(17×→8.8×→1.7×) 꼬리 ≥ 0.33, 최근 3봉 극값을 지킴 — 클라이맥스 봉 자체에서는 발화하지 않는다(그때는 아직 가속 중), 또는 (b) 3봉 변화율이 직전 3봉의 35% 이하(직전 ≥ 0.3%). 두 분기는 방향이 어긋날 수 있다 — 한 봉이 (a)로 DIP, (b)로 POP를 동시에 내는 경우가 2026-08-29 신호-초 122개 중 11개(9%): 외검에선 게이트가 담기·덜기를 거의 배타로 걸러 무해하고, 그런 신호는 `dbl=True`로 기록돼 야간 분할에 나온다. 후보 규칙("한 봉은 극값 하나만, 종가 방향이 정한다")은 그 표가 며칠 갈릴 때만.
  - `s8`(**기록 전용, `s8_on` 0**): legs 표의 후보 검출기를 Features 에 그대로 넣은 것 — `s8_h`(8)초 이동을 ATR 로 잰 속도가 그 다리의 최강 푸시의 `s8_rebuild`(0.5) 이상으로 다시 선 뒤 그 푸시의 `s8_decel`(0.3) 이하로 죽으면(깊이 조건 `dip_min_atr` 동일, 발화 간 `s8_cool` 60초) 발화. 스케일이 σ가 아니라 다리 자신의 푸시라 1%/분 슬라이드(σ 단위로는 v ≈ 0.5)의 감속을 본다(legs 08-30·31: actionable 42~62% vs v 10~31% / 1m 18~21%). `s8_on` 0이면 신호에 `shadow=True`가 붙어 Strategy가 무시하고 공통 쿨다운(`d["last"]`)도 건드리지 않으므로 live 는 비트 단위로 동일하다 — replay/legs/야간 표가 v·1m·s8 을 나란히 비교하기 위한 것. **매매 소스로는 이득이 없다**(2026-09-02 최종 코드, 기준 테이프 `--fixed`, 04–06 / 07–10 / 27h / 08-31): 현재 −5.46 / +2.27 / +1.25 / +6.60, 1m·v 에 s8 을 더하면 −2.45 / +1.07 / +2.77 / +2.15(사이클 +25~40%; 급락일·27h 는 낫고 두 방향 장·08-31 은 나쁘다 — 날짜별 재현 실패), 1m 대신 s8 이면 −8.53 / +1.63 / −3.23 / +6.16(4/4 열세; 급락일엔 ATR 부풂으로 깊이 조건이 안 열려 5 사이클). "어느 멈춤인가"의 손잡이 `s8_hold`(죽은 속도의 지속 초)·`s8_vd`(공격자 거래량 소멸 확인)는 코드에 있고 결과는 NEXT 1. 극점에 더 가까운 발화가 돈이 되지 않는 이유는 NEXT 1 의 열린 질문이다(사다리가 한 다리에 한 발화만 통과시키므로 "어느 멈춤인가"가 문제지 "얼마나 가까운가"가 아니다).
  - 공통 쿨다운 60초(또는 1 ATR 더 진행). BREAKDOWN/BREAKOUT은 신호도 담기도 막지 않는다(CONCEPT: 급락 뒤 첫 감속을 막는 규칙은 두지 않는다 — 브레이크의 정의가 "거래량 실린 30분 신저가"라 범위장의 항복 저점이 바로 그것이다; 2026-08-29 live 19:07–10 거부 3건 뒤 +1.2%). 브레이크는 **보유 중인 캠페인을 향해 발생했을 때 derisk의 근거**가 될 뿐이다. 브레이크 중 담기에 `add_confirm`을 요구하는 중간안은 테이프에서 거부 삭제와 체결이 동일해(1분봉 망치 뒤엔 되돌림 조건이 늘 충족) 대안이 아니다.
- **BREAKDOWN / BREAKOUT** — 60초 전까지의 30분 극값을 0.3 ATR 넘어서고 60초 거래량이 10분 평균의 2배. 5분 유지. 시작 시 30분 창을 캔들 종가로 채워 재기동 직후 오탐을 막는다.
- 확인용 피처(게이트 아님, 신호마다 기록): CVD 다이버전스, 호가 재충전, 매수비중, 30초 매수/매도 체결량, 감속비, 캔들 vr/꼬리/roc3, VP 밀도·가치영역·HVN 거리, 레짐 블록, **거래량 소멸**(`sell_spike`/`sell_decay`/`sell_now`, 매수 쪽 동일: 10초 창의 공격자 체결량이 10분 평균의 `vd_spike`(2)배 이상 스파이크 뒤 그 절반(`vd_decay`) 이하로 줄고 아직 줄고 있음). `vd_gate`=1이면 게이트가 된다(눌림엔 매도 소멸, 반등엔 매수 소멸 필요) — 기본 0(기록만), 야간 리포트에서 갈리면 승격.

## 상태기계 (`Strategy`, 방향 상대적 — 숏은 부호 반전)
- **담기**: 담기 쪽 신호 → 즉시 최우선 호가에 post-only. 추격 없음. 대기 주문은 신호가 유효한 동안만 정당하고 세 가지로 거둔다: (1) **떠남** — 신호가의 mid에서 `pop_min_pct`(0.4%)만큼 반등하면 취소(`DISARM left`: 감속이 예측한 반등이 우리 없이 일어났다; 그 뒤 이 가격의 체결은 감속 판독 없는 새 하락에서의 체결이고, 되돌림에서 감속이 다시 나오면 새 신호가 새 주문을 낸다), (2) **재가속** `s·v ≤ −cancel_v`(1σ)면 취소, (3) **TTL `buy_ttl_s` 90초** — 백스톱이 아니라 실제 필터다: 라이브 48건은 41건이 15초 안, 5건이 21~86초(저점 스톨: `v`는 전이에서만 발화하므로 대기 주문이 스톨을 사는 유일한 길), 90초를 넘긴 2건은 +0.25%/+0.66% 반등 뒤의 되돌림이었고, **90초를 넘겨 두면 되돌림 체결이 다음 신호의 더 낮은 체결을 선점한다**(2026-08-30 테이프, `left` 밴드를 켠 채: 27h −13.30 → 300초 −16.86 / 900초 −15.46 / 1800초 −15.46, 저녁 +2.76 → +1.50, 기준 04–06은 불변; 첫 분기 08-30 03:47 대기 주문이 2.609에 체결 vs 90초 규칙은 03:50 새 감속에서 2.602). 90초 안에서는 밴드가 테이프상 아무것도 바꾸지 않는다(빠른 V 반등 때 남은 초를 기다리지 않고 거두는 것뿐). 체결되면 arm 해제(신호 하나 = 유닛 하나). PLACE 이벤트의 `queue`는 그 가격에 먼저 서 있던 **표시량**이고 `mid`는 도착 mid 다(TAKER·FILL 에도 `mid`, 2026-09-02 — oid 로 이어 슬리피지·임팩트를 재는 기록, NEXT 6). 표시량은 앞줄이 아니다: live 메이커 861건에서 체결 전까지 표시량의 1.4배(덜기)~5배(담기)가 취소로 빠졌고 체결 시점에 실제로 거래된 양은 표시량의 중앙값 2%(TRUMP)였다 — 미체결의 1차 원인은 시간이 아니라 소진되지 않은 큐라는 판독(2026-08-30 16:14: 8,600계약 뒤, 119초 뒤 관통)은 그 큐를 취소가 갉아먹는 속도까지 봐야 한다. 게이트: 보유 중 추가는 직전 매수가 대비 `step`(= max(`step_add_pct` 0.5%, `step_add_atr` 0.4 × ATR15), TRUMP ≈ 0.9%; `step_add_max_pct` > 0 이면 그 상한 — 급락 뒤 ATR15 가 6시간 ×1.5 부풀어 step 이 1.24% 로 벌어지는 것의 손잡이인데 테이프에선 저녁 07–10 에서만 +1.56 이고 나머지 셋과 바구니 테이프에선 무변화라 0(꺼짐), NEXT 1) 아래 — 고정 레벨 대기 추매는 없다(그건 그리드), 보유 중 덜기 뒤 재매수는 덜기가 대비 `gap_rebuy_pct`(0.3%) 아래(재고 순환의 안티-churn: 판 유닛을 더 싸게 되사야 사이클이다). **포지션 0이면 앵커가 없다** — 마지막 물량을 판 뒤든 손절 뒤든 재진입은 다음 감속 그대로(CONCEPT "다음 감속에서 다시 산다"; 이전 구현은 덜기가·손절가 −0.3% 아래에서만 재진입해 2026-08-29 live에서 1.5시간 동안 감속 7개를 흘려보냈고, 손절 뒤엔 시장이 손절가 아래로 더 내려와야만 재진입했다), `max_units`(4)·`max_notional`(수량 기준 — 부분 체결 중인 유닛은 한 유닛으로 세지 않는다), **cap**은 예산이 아니라 거래소 스탑 자체다(B: 추가마다 `평단 − cap/qty`로 재계산되어 위로만 움직인다; 스탑 절 참조), live에서 가용증거금과 프라이빗 피드 생존. arm 진행은 순포지션이 아니라 그 주문의 체결량으로 센다(arm 중 덜기가 체결돼도 유닛 하나만 산다). **감속 없이는 절대 안 담는다.** 담기는 **메이커 전용**이다 — 미체결 담기를 N초 뒤 테이커로 바꾸는 규칙은 두지 않는다(2026-08-30 검증: "메이커가 안 체결되는 건 가격이 떠난 V자뿐이니 대상이 좋다"는 가설로 10초/0.3 ATR 밴드를 테이프에 걸었더니 저녁 +0.54 → −1.7, 밴드·시간 변형 전부 악화, 하한 밴드(0.5 ATR) 변형도 중립 이하 — 10초 뒤 호가가 한두 틱 위인 보통의 경우까지 스프레드+수수료를 내며 전환하고 진짜 V자는 드물다. 덜기의 테이커 전환은 놓치면 손실이라 유지). 떠난 가격은 다음 감속에서 다시 산다; 바닥에 더 가까운 신호는 속도 모델 고도화의 몫이다.
- **사이징(자본 비례, 복리)**: `unit_frac`(1.5) > 0이면 유닛 명목가 = 지갑 자본(equity − 미실현) × unit_frac, `cap_frac`(0.15)·`daily_loss_frac`(0.3)·**`notional_frac`**도 같은 자본 기준. **자본에 비례하지 않는 한도를 남기지 않는다**(CONCEPT: "그 상한은 자본에 비례한다") — 고정 `max_notional`은 지갑이 복리로 커지면 낡아서 스케일된 유닛보다 작아지고 책을 굶긴다(2026-09-01: 지갑 146 → 728로 유닛이 546/책이 되자 `max_notional` 900의 절반인 450/책에 걸려 2.5시간 동안 신호 대부분이 `SKIP max_notional`). `notional_frac = max_units × unit_frac`이면 어떤 지갑 크기에서도 사다리 한 벌이 정확히 들어간다. `notional_frac` 0이면 고정 `max_notional`로 되돌아간다(기본값). 포지션 0일 때만 60초마다 재계산, 한 번에 ±25% 이내, arm 시점의 유닛 크기는 그 유닛이 다 찰 때까지 고정. 고정값(`unit_qty`·`cap_usdt`·`daily_loss_limit`)은 frac이 0일 때의 대체값이자 시작값. 손실 뒤엔 유닛이 줄어드는 안티-마틴게일이 목적의 절반이다.
- **덜기 (주)**: 덜기 쪽 신호가 뜨면 LIFO 유닛을 최우선 호가 메이커로, 10초 내 미체결이거나 pull 시작가 대비 `trim_taker_slip_pct`(0.1%) 이상 밀리면(정체가 이미 꺾임) 테이커. pull은 게이트 아래로 **한 틱 이상** 밀려야 버린다(히스테리시스; 메이커 주문은 어차피 게이트 이상에서만 체결되므로 한 틱 흔들림에 취소·재등록하면 큐 순위만 잃는다 — 2026-08-30 03:17 pull/drop 6회 반복 뒤 테이커로 나간 사례). 기준 가격은 유닛마다 다르다 — **추가한 유닛은 그 유닛의 매수가 + `unit_min_pct`(0.15%, 수수료 커버)**: 평단에 못 미쳐도 저점 반등 정체에서 던다(그 유닛의 왕복 수익이 코어에 얹히고 평단이 내려간다). **코어(마지막 `core_units` 유닛)는 갱신된 평단 + `pop_min_pct`(0.4%)**. 평단 대비 `full_exit_pct`(3%) 이상이면 전량. 진입가·고정 목표에 앵커하지 않는다. pull은 만들 때의 로트만 판다 — pull이 살아 있는 동안 담기 주문이 체결돼 포지션이 커지면 pull을 버리고(`PULL_DROP why=add`) 다음 정체에서 새 LIFO 로트 기준으로 다시 판단한다(목표 수량이 절대값이라 새 유닛까지 자기 가격 아래에 팔던 결함, 2026-08-29 퍼즈). pull 수량은 거래소 수량 단위(`qstep`)의 정수배이고 잔량이 반 단위 이내면 완료다 — 76.1의 반 38.05를 시키면 38.0만 체결되고 0.05가 5초마다 40017로 거부되며 그 좀비 pull이 이후의 모든 덜기를 막았다(2026-08-29 21:37 live). OMS도 한 단위 미만의 시장가는 내지 않는다.
- **회계**: 거래소 방식(Bitget openPriceAvg). 평단은 담을 때만 움직이고, 덜면 실현손익 = (체결가 − 평단)×수량, 평단 불변. 저점 유닛을 평단 아래에서 덜면 장부엔 실현 손실로 찍히지만 자산은 늘고 평단은 유지된 채 물량만 줄어든 상태 — 이것이 순환매의 정상 사이클이다.
- **게이트 완화(시장이 반등을 거부한 횟수)**: 덜기 쪽 정체가 그 로트의 게이트에 못 미치면 실패로 세고, 실패마다 게이트를 바닥(추가 유닛 = 자기가 + max(`gate_floor_unit_pct` 0.05%, 왕복 수수료 `fee_rt_pct` = 계약의 메이커+테이커 ≈ 0.08%) — CONCEPT "그 물량의 왕복은 이익"은 테이커 출구까지 성립해야 한다; **코어도 왕복 수수료 `fee_rt_pct`**(사용자 결정 2026-09-02: 바닥이 0이면 거부가 쌓인 코어를 본전에 팔아 수수료만큼 실현 손실을 내는 덜기가 가능했다))까지 `gate_relax`(0.5)의 비율로 낮춘다. **평단 기준 출구(2026-09-01)**: 추가 유닛이 평단보다 나쁜 자리에 남아(사이클이 성공해 더 싼 로트가 덜리면 avg 는 거래소 회계라 그대로 남는다) 로트 기준으로는 못 파는데 포지션은 이익이면, 그 로트를 **코어와 같은 게이트**(`pop_min_pct`, 바닥 = 왕복 수수료, 같은 거부 카운터)로 평단 대비 판다 — 돈은 평단 회계이고 CONCEPT "먹었던 이익이 본전으로 돌아오게 두지 않는다"의 구현이다. `dev ≥ 0`이 조건이라 실현 총손익이 음수인 덜기는 만들지 않는다(수수료는 코어와 같은 취급). 발화 예(live 2026-09-01): 로트 [225@2.418, 225@2.387]·avg 2.3597·mid 2.3655 — 평단 +0.246%(덜면 +1.31)인데 로트 −0.901%라 거부되던 자리. 기준 테이프 4개 전부 소수점까지 무변화이고(그 국면이 27h 중 204초 = 0.11%뿐, 덜기 신호가 겹친 초는 3), 근거는 측정이 아니라 메커니즘이다(NEXT 5). 초기화는 **매수 체결이나 LIFO 로트 교체**(한 로트가 다 팔려 다음 로트로)에서만 — 같은 로트의 부분 매도는 그 로트의 거부 이력을 지우지 않는다(2026-08-29 20:56 live: 전량 pull 76.1 중 9.2만 메이커 체결되자 잔량 66.9의 게이트가 0.2에서 0.4로 되돌아갔던 결함). 시간이 아니라 무산된 기대의 횟수가 기준이다(CONCEPT). derisk 게이트가 더 낮으면 그것이 우선. **되돌림 고점도 낮아진 게이트를 쓴다**(설계): 거부 한 번에 게이트가 0.4→0.2가 되면 그 전에 찍은 +0.35% 고점이 게이트를 넘은 것이 되어 다음 초에 되돌림 매도가 나갈 수 있다(2026-08-29 live 18:39: +0.18%에 전량, 이후 +0.8%). 메커니즘은 그대로이고 공격성은 숫자다 — `gate_relax`·`trim_retrace_atr`는 튜너 격자에 있다.
- **담기 확인 `add_confirm` — params 0(꺼짐), 쌍검에서도**: 켜지면 담기 신호 하나로는 부족하고 (a) 두 규칙(`v`·`1m`)이 `confirm_within_s`(90초) 안에 같이 발화, (b) 그 움직임의 공격자 체결량 소멸(`sell_decay`/`buy_decay`), (c) 극점에서 `trim_retrace_atr`×ATR 이상 되돌림 — 셋 중 하나가 필요. 코드의 자동 규칙(None이면 쌍검에서 켜짐)은 남아 있지만 params가 0으로 고정한다: 2026-08-30 테이프에서 쌍검 confirm on은 off 대비 27h −4.0 / 급락 −3.5 / 저녁 −0.7(−1.34/−6.84/+1.71 vs +2.70/−3.30/+2.42) — 사이클을 잃는 비용이 나쁜 진입을 거른 값보다 컸다. "리스크를 여는 쪽이 더 높은 기준"은 사다리 기하(담기는 한 step 아래 감속에서만, 덜기는 원가 위 정체에서)에 이미 있다.
- **일봉 역행 사이즈**: `daily_trend`(일봉 종가 > EMA20 & EMA20 5일 상승 = up, 반대 = down, 아니면 None; 매시간 갱신)에 book 방향이 역행하면 유닛에 `against_daily_mult`(**params 1.0 = 꺼짐**, 2026-08-30: 08-19 펌프의 EMA20 잔상으로 몇 주 'up'이라 숏 책만 반으로 줄이던 것 — 판독 정의는 NEXT 1과 함께; 코드 기본값 0.5). 거부권이 아니라 사이즈 스케일이다. 방향 힌트(`side_hint`)는 **1H 구조**(15분봉 집계)가 1차, 20봉 미만이면 15분 구조.
- **전량익절 우선(신성한 코어 없음)**: 평단 대비 `full_exit_pct`(3%) 이상에서의 정체는 FAVOR·derisk의 코어 보류와 무관하게 코어 포함 전량을 판다. 코어 보류는 그 아래에서만 적용된다.
- **유닛 수량**: 유닛 = `unit_qty` × 일봉 배수를 arm 전에 거래소 수량 단위(`qstep`)로 반올림한다. 한 신호 = 한 유닛이 부분 체결·반올림 잔량으로 깨지지 않게.
- **덜기 (되돌림 고점)**: 마지막 체결 이후 최고 유리가가 덜기 게이트 위였고 거기서 `trim_retrace_atr`(0.5)×ATR **이상이면서 그 반등(마지막 체결 이후 최악가→최고가, `peak − trough`)의 `retrace_frac` 이상** 되돌리면 정체 신호 없이도 즉시 pull(윅 보호; 고정 가격 없음, 1초 반응). ATR 항만으로는 조용한 종목에서 0.02~0.06%(2026-09-02 바구니의 0.5×ATR1m)라 게이트에 닿은 뒤 첫 흔들림에 팔았고 그것이 pull 의 63%(retrace 243 vs stall 36)였다 — 고정 익절과 다를 게 없는 출구였다. 되돌림이 반등의 고정 비율이어야 "고점이 확인된 것"이다. `trim_retrace_atr` 0이면 둘 다 없음, `retrace_frac` 0이면 ATR 항만. **params 는 2026-09-02 10:19부터 `retrace_frac` 0.2**(코드 기본 0.33; 사용자가 예외로 손으로 옮김 — 최종 체결 모델 + 레그 판독 기준에서 0.2 가 +0.88 / +3.67 / +9.06 / +4.46 vs 0.33 의 −1.66 / +3.44 / +5.04 / +6.44, 3승 1패; 튜너 격자 안의 값이라 튜너가 다시 옮길 수 있다).
- **덜기 (백스톱)**: `trim_rest_pct`(기본 0 = 없음; 사용자 결정 2026-08-29 — 대기 익절은 그리드라 제거, 출구는 정체 신호·전량익절·derisk·스탑뿐). 0보다 크면 LIFO 유닛이 평단 + 그 %에 대기.
- **derisk**: AGAINST 레짐(`derisk_on_against`, 기본 true; **params는 false** — 2026-08-30: 90분 후행 창의 라벨은 정의상 늦어(10:51 AGAINST는 10:25 저점 26분 뒤, 09:15 반등 스윙이 창을 빠져나가며 켜짐) 회복 국면의 약반등에서 코어를 자를 수 있으므로 라벨은 사이즈 스케일로만 쓰고 derisk 근거는 시의성 있는 둘(래치·보유 중 브레이크)만), 또는 **보유 중에 발생한** BREAKDOWN(그 5분 플래그 동안; 브레이크 뒤의 감속에서 산 유닛은 그 브레이크의 피해자가 아니다 — 근거는 캠페인보다 뒤에 와야 한다. 이전 구현은 브레이크 중에 산 유닛을 4분 뒤 본전 정체에서 반 팔았다), 또는 평단 대비 −`step` 이상 빠졌는데 담지 못한 상태(감속이 없었음), 또는 **구조가(전제) 너머로 갔던 상태**(`prem_broken`, B — 스탑 절) → 부분 손절: 약반등 정체에서 덞. **피해 근거는 래치든 브레이크든 같은 조건으로 풀린다: 체결(감속이 와서 담았다면 캠페인은 막힌 게 아니다) 또는 정상 게이트까지 회복** — 2026-08-29 21:08 live: 브레이크(2.7135) → 18초 뒤 감속 담기 2.708 → 유닛은 2.720에 정상 순환했는데 브레이크 근거가 살아남아 코어 66.9를 평단+0.03%에 전량 던진 결함. 약반등(정상 게이트 미만)에서만 derisk 수량이고, **정상 게이트 이상의 정체는 derisk 중에도 정상 덜기(로트 전량)**다. **되돌림 고점 출구는 derisk 완화를 쓰지 않는다**(`gate_top`, 2026-09-02): 완화된 −`derisk_pct` 아래에서는 "고점"이 평단을 한 번 스친 것이 되고 되돌림 조건(`trim_retrace_atr`×ATR)이 **진입가 아래 트레일링 스탑**으로 퇴화해, 래치가 켜지는 순간 이미 참이라 즉시 발화한다 — 09-01 live derisk 체결 10건 중 6건이 최유리점에서 0.49~0.92% 반대쪽이었고 `dev`가 arming 문턱(−`step`) 바로 아래(−0.51~−0.70)에 몰렸다. 즉 진입 조건이 곧 체결가였다. 고친 뒤 되돌림 출구는 그 로트의 **정상(완화된) 게이트 위**에서만 발화하고, 그 아래에서는 정체 신호만이 derisk 수량을 낸다 — CONCEPT "순환매의 매도는 언제나 정체에서". 추가 유닛(LIFO)은 언제나 **자기 매수가 + 0.15% 위에서만**(손실 실현 없음), 코어는 **평단 − `derisk_pct`(3.0% ≈ 거리 제한 없음)** 까지 손실이어도 `derisk_core_frac`(0.5)만큼 — 반, 그리고 나머지: 남을 물량이 1/4유닛(`unit × (1−frac)²`) 이하면 통째로(반을 거듭 자르면 0.27계약 먼지가 남아 죽은 캠페인의 스탑·gap 앵커·브레이크 근거를 살려둔다 — 2026-08-29 백테스트에서 그 먼지가 옛 스탑 2.762를 새 진입 2.952에 물려줬다; 기준을 반유닛으로 두면 1유닛 미만 코어가 첫 컷에 전량 나간다 — 21:11 live). **유닛이 코어 위에 얹힌 채 물린 상태(2로트 이상)에서도 같은 컷이 나간다 — 코어 로트에서**(2026-09-02): LIFO 유닛은 자기가 위에서만 팔리므로 약반등의 손실은 코어 로트(`lots[0]`)에서 감수한다(CONCEPT "손실은 평단 기준의 물량에서 감수"). 거래소엔 어느 계약이든 같은 매도이고 엔진의 로트 장부만 다르다 — 주문·체결(`FILL.lot="core"`)·`bot.cycles`가 그 로트를 따라간다. 이전엔 이 컷이 1로트에서만 가능해 노출이 가장 큰 상태에 부분손절이 없었다(live 컷 98건 전부 1로트). 스위치 `derisk_under_units`(1; 0이면 옛 동작). **테이프 비용(고정 사이즈, 04–06 / 07–10 / 27h / 08-31)**: 이 컷 없이 −3.52 / +2.16 / +6.27 / +1.33, 컷을 켜면 −4.08 / +2.16 / +0.82 / −0.32, **derisk 전부 끄면(`derisk_pct` 0) −2.77 / +3.07 / +7.31 / +2.04** — 부분손절 계열은 급락일을 포함한 네 테이프 전부에서 비용이다(급락은 cap 이 끝내고, 컷은 회복 반등의 바닥에서 나간다). 2026-09-02 최종 체결 모델 + 레그 판독 켠 기준에서도 같다: 현재 −1.66 / +3.44 / +5.04 / +6.44, 컷 없이(`derisk_under_units` 0) −1.06 / +3.44 / +11.13 / +9.03, derisk 전부 끄면 −0.64 / +4.46 / +10.13 / +9.77 (NEXT 3). **그래서 params 는 2026-09-02 10:19부터 `derisk_pct` 0·`derisk_under_units` 0 — 부분손절 계열 전체가 꺼져 있다(사용자 결정, live 실험; CONCEPT 리스크 절에 적음).** 이 절의 메커니즘 서술은 켰을 때의 계약이다. 실험 규약과 되돌리는 조건은 NEXT 3. 전제(느린 원웨이)가 있는 테이프가 없으므로 메커니즘은 CONCEPT 대로 두고 깊이·근거는 NEXT 3. `PULL_TRIM.mode`는 **발효 중인 게이트**다: derisk 는 코어 컷(약반등, −derisk_pct 게이트)일 때만이고, derisk 근거가 켜진 채 정상 게이트에서 나가는 유닛 매도는 normal 이다(옛 로그의 derisk 108건 중 10건이 이것).
- **스탑 (B, 사용자 결정 2026-08-30 20:40) — 두 층**. (1) **거래소 pos_loss(mark 기준) = 돈 한도만**: `stop = 평단 − s·cap_usdt/max(qty, unit)`(첫 진입 주문의 프리셋 SL도 같은 식). 분모가 체결량이 아니라 최소 한 유닛인 이유는 부분체결(cap을 4.3계약으로 나누면 롱 스탑이 −0.16 → 43011 거절 ×3 → 불필요한 시장가 청산 + HALT, 2026-08-31 18:16); 유닛보다 작은 물량의 스탑 손실은 qty/unit × cap. 재난용이다: 엔진이 죽어도 작동하고 거리상 헌팅 면역이다. 물량이 적을 땐 넓고 채울수록 조여진다(cap 13.2·유닛 $131 기준 1유닛 −10%, 2유닛 −5%, 4유닛 −2.5%). 담기는 이 스탑을 **위로만** 옮긴다(추가 후 손절가 손실 = cap 으로 재계산). 한 번 만든 스탑은 포지션에 불리하게 절대 옮기지 않는다(가격이 닿으면 손절이지 재계산이 아니다) — **래칫**. 재계산이 더 낮은 스탑을 내는 경우는 있다: 덜기로 수량이 줄고 더 낮게 다시 담으면 `평단 − cap/qty`가 기존 스탑 아래로 내려간다. 그때 래칫이 기존(더 가까운) 스탑을 지키므로 **사이클을 돈 캠페인의 실효 cap 은 `cap_usdt`보다 작다**(2026-09-02 감사 7: ZEC 에서 그 래칫이 스탑 아래로 3.75% 더 내려간 날 약 7.6 을 아꼈다). 이것은 계약이다 — 먹은 사이클이 다음 캠페인의 한도를 조인다(CONCEPT "먹었던 이익이 본전으로 돌아오게 두지 않는다"의 스탑 쪽). 풀 cap 으로 되돌리려면 그 자리에서 규칙을 바꿔야 하고 지금은 바꾸지 않는다. 유효한 스탑이 존재할 수 없으면(가격이 이미 cap 너머) 즉시 청산. 마지막 방어선: `guard()`는 어떤 경로로 계산됐든 1틱 미만의 스탑 요청을 1틱으로 올린다(거래소 트리거는 양수여야 한다). (2) **구조가(캠페인의 전제)는 소프트다**: 15분·1H 피벗 저점(`Features.htf_lows`: 15분봉·1H봉 zigzag 확정 피벗의 저가 + 진행 중인 다리의 저점) 중 **남은 사다리 자리를 남기는 가장 가까운 것** − `stop_buffer_atr`(0.3) ATR15 — 마지막 매수가 아래로 (max_units − 보유 로트 수) × step 이상 떨어진 저점만 후보(`signal.structural_level`; `stop_structural_on` 1일 때, `stop_structural`이 숫자면 그것). 진입 때 후보가 없으면 **나중에 자격을 얻는 첫 피벗을 전제로 잡는다**(피벗은 확정에 지연이 있다; 2026-09-02 — 이전엔 `adopt_stop`이 거래소 스탑 값을 구조가 자리에 써넣어 re-sync·청산가 가드·재기동 뒤엔 전제가 cap 이 되고 새 구조가를 못 잡았다). 위로만 트레일(`stop_trail`=1 또는 FAVOR; 확정된 더 높은 피벗 저점 아래로, 한 틱 이상 조여질 때만). 가격이 구조가 너머로 가면 `prem_broken` 래치가 켜져 derisk 근거가 된다(약반등에 코어 절반, 다음 반등에 나머지; 래치는 체결 또는 정상 게이트 회복에서 풀린다) — 거래소 스탑은 그대로다. 근거: 스탑 4건 중 3건이 헌팅(08-30 19:13 live: 스탑가 아래로 다시 안 옴, 2시간 +3.2%); 대가는 진짜 이탈을 되돌림 매도가 나올 때까지 타는 것이고 최악은 cap. 1분봉 피벗(`struct_lo`)은 손절에 쓰지 않는다 — 감속 매수는 그 저점 바로 위라 손절이 첫 추가 자리보다 위에 놓인다(08-29 live 첫 포지션: 진입 2.742, 손절 2.718 = −0.88%, 첫 추가 자리 ≈ −1%). **청산가 가드**: 스탑은 절대 청산가 너머에 두지 않는다 — 거래소의 liquidationPrice가 **양수이고 포지션의 손실 쪽에 있을 때만** 그 값 × 1.01(크로스에선 계정 단위라 반대쪽 손익에 따라 움직이고, 전 계좌가 받쳐 양수 가격대에 청산이 없으면 Bitget은 **음수 센티넬**을 준다 — 그건 가드 없음이다; 08-30 23:53 그 음수를 스탑 요청값으로 보내 43011 을 4분간 232번 받았다), 격리에서만 폴백 `기준가 × (1 − 0.9/레버)`; 크로스에서 유효한 청산가가 없으면 가드 없이 돈 한도 그대로(`STOP_LIQ_GUARD`는 1~2유닛의 정상 상태라 events.jsonl에만). 백테스트는 레버를 모델링하지 않아 이 가드가 없다. **OMS**: 스탑 없는 포지션은 존재하지 않는다 — 첫 유닛 진입 주문에 프리셋 손절(`presetStopLossPrice`)을 실어 체결 순간부터 보호하고, 전체 포지션 pos_loss가 등록된 뒤에야 그 프리셋을 정리한다(반대 순서 금지). 등록은 책마다 잠금 하나로 직렬화한다(체결의 reconcile 과 housekeeping 의 ensure_stop 이 동시에 "없음"을 보고 둘 다 내는 경합). 기존 스탑의 수정이 실패하면 기존 스탑을 남기고 알림. 등록 시 40917("스탑가 < 마크가여야")은 가격이 이미 손절선을 지났다는 뜻 → 즉시 시장가 청산(손절로 회계). 스탑이 아예 없는데 3회 등록 실패면 시장가 청산 + HALT.
- **손절 후**: HALT 아님. `stop_cooldown_s`(300) 뒤 다음 감속에서 재진입(가격 앵커 없음 — 손절가 위든 아래든). 하루 `max_stops_day`(3)회면 HALT(DAILY_STOPS).
- **레짐** (1분봉 90개, 서킷브레이커): 원웨이 = 강한 drift(≥ 6 ATR) **그리고** 반대 방향 0.7% 스윙이 `rg_counter_max`(1)개 이하. 1%짜리 눌림이 20분마다 나오는 추세는 원웨이가 아니라 사이클 장이다. AGAINST(내 반대) 3분 연속 → **담기는 끄지 않고 유닛 × `against_regime_mult`(0.5; 일봉 역행 배수처럼 유닛 수 상한도 같이 줄어 최대 노출 2유닛 상당)** — 거부권이 아니라 사이즈 스케일(사용자 결정 2026-08-30: "거부권은 아닌 것 같다"); 라벨 자체는 `rg_drift_min_pct`(2.0) — 창의 순이동이 그 % 이상(사다리 깊이 규모)일 때만 AGAINST/FAVOR(ATR이 작은 조용한 장의 −1% 미끄러짐이 −7 ATR "원웨이"로 읽히던 것; FAVOR도 대칭). 코드 기본값 0/0은 종전 동작(거부권·ATR만). 해제는 반대 스윙이 다시 생기거나 drift가 절반 이하로 식을 때. 2026-08-30 관찰: 새벽 −1.4% 미끄러짐(ATR 0.1%)에서 06:43 AGAINST → 담기 9건 거부 → derisk가 바닥에서 잔량 정리 → +1.1% 반등은 무포지션. 녹화 테이프 셋(캐스케이드·저녁·새벽)에서 거부권이 무언가를 지킨 적은 없고(캐스케이드 날엔 0.7% 반등이 흔해 안 켜짐), 스윙 문턱을 ATR로 바꾸면 캐스케이드 바닥에서 켜져 회복 사이클을 막는다(−16.3 → −20.3, 기각). FAVOR(내 방향) → pop_min ×2, 소프트 구조가 트레일; **코어 면제는 없다**(CONCEPT "신성한 코어는 없다"; 90분 후행 라벨은 정의상 늦고 live 영향은 pull 1건이었다 — 사이클 척도의 코어 홀드는 NEXT 5의 증거로만). DEAD = 스윙 < 2 → 라벨만. 자동 사이드 플립 없음. **빠른 캐스케이드의 방어는 브레이커가 아니라 구조 손절·cap이다**: 2026-08-29 −10%/15분 캐스케이드에서 AGAINST는 안 켜졌고(90분 창에 펌프의 상승 스윙이 남아 fav_n>1), 더 빨리 켜지게 바꾸면(예: 창 극값 이후 다리 안의 반대 스윙만 세기) 손절(15:07)은 못 막으면서 그 뒤 첫 감속 진입 15:13·15:24(둘 다 이익)를 막는다 — 원웨이 테이프가 더 쌓이기 전엔 메커니즘을 건드리지 않고 `rg_drift`·`rg_counter_max`·`rg_window`만 튜너 격자에 둔다.

- **현재 레그 판독(`rg_leg_on`, 2026-09-02; 기본 0 = 기록만)**: 90분 후행 창 대신 **마지막 확정 θ(`rg_theta`) 피벗부터의 레그**를 본다(`Features._structure`, 1분봉 종가, `stop_lookback` 창). 레그 안에는 정의상 θ 반대 스윙이 없으므로 레그가 `rg_leg_pct`(2%) 이상 깊으면 "원웨이 진행 중"(`leg_ow` = ±1)이고, 다음 θ 스윙이 레그를 끝내는 순간 풀린다 — 창을 기다리지 않아 움직임 안에서 켜지고 첫 진짜 반등에서 꺼진다. 항상 기록된다(`f.leg_dir/leg_pct/leg_min/leg_ow`, replay `--by leg_ow`, backtest `--follow leg`). `rg_leg_on` 1이면 이 라벨이 창 라벨의 AGAINST/FAVOR 자리를 받는다(DEAD/TWO_WAY 는 스윙 수 그대로, `rg_confirm` 적용, AGAINST 히스테리시스는 없음 — 해제가 곧 반등이다). 용도는 창 라벨과 같이 **사이즈뿐**(`against_regime_mult`, FAVOR 의 pop_min ×2·트레일): TRUMP 캐스케이드에서 05:54·06:03 UTC 에 켜지고 06:16 바닥 확정에 꺼졌으며(창 라벨은 급락 내내 침묵, 저점 26분 뒤), 기준 테이프 4개에서 역방향 절반 축소가 −5.46 → −1.66 / +2.27 → +3.44 / +6.60 → +6.44 / +1.25 → +5.04(DD 전부 감소). 한쪽 플립(`--follow leg`)은 급락일 −13.4 / 27h −5.3 로 콜임이 다시 확인됐다 — 플립에는 쓰지 않는다. 2.5% 규모의 급락엔 2% 문턱이 움직임의 끝에서 켜진다(08-30 10:14 UTC, live 손절 1분 뒤): 판독이 사는 것은 큰 꼬리의 안쪽 절반이다.
- **방향(롱/숏 순환매)**: `strat.side`는 에이전트가 포지션 0일 때 정한다(변경 = 자동 재기동). 원칙: 작은 시간대의 흔들림을 먹되 방향은 큰 시간대 구조를 따른다 — 롱의 담기(눌림)는 상승 드리프트가, 숏의 담기(반등)는 하락 드리프트가 구해준다. 판독기 `side_hint`: 15분 종가 지그재그(임계 max(1%, 2×ATR15%))의 마지막 피벗 둘씩이 고점↑·저점↑이면 long, 고점↓·저점↓이면 short, 섞이면 None(유지). 현재 side와 45분 이상 어긋나면 `SIDE_HINT` 알림(시간당 1회). 자동 전환은 아직 없다(힌트가 실적을 쌓으면 스위치로).
- **쌍검술(롱·숏 순환매 동시) — 2026-08-30 22:36부터 live의 기본 형태(사용자 승인)**: 눌림 감속의 매수는 롱의 담기이자 숏의 덜기, 반등 정체의 매도는 롱의 덜기이자 숏의 담기 — 두 사이클은 같은 순간 같은 방향으로 움직이고 차이는 재고 제약뿐이다. 단독 사이클이 물량 0으로 흘려보내는 반대편 정체를 쌍검은 사이클로 만든다. 방향당 예산(유닛·cap·일일 한도·명목)은 절반, 방향별 Book(같은 Features, 별도 포지션·스탑·접두사 `cycL-`/`cycS-`). **쌍검 = 두 방향의 평균이라 방향 판독이 필요 없다**: 27h/급락/저녁 +2.70/−3.30/+2.42 vs 롱 고정 −4.90/−9.14/+3.07, 숏 고정(풀) +10.03/+2.54/—; DD 10.8 vs 25.5. 비용은 원웨이에서 역방향 책의 손실(급락일 −3.3 = 롱 고정의 1/3; 방향당 하루 3회 HALT). 추세 쪽을 키우고 역방향을 줄이는 기울임은 판독이 제때일 때만 값이 있다 — 15m/1H 구조·레짐 라벨로 기울이면 이 테이프에선 손해(늦은 판독이 급락 내내 숏을 반으로): 기울임 없음이 현재 계약, 재판정은 NEXT 2. `add_confirm`은 0(위 담기 확인 절), `against_daily_mult`는 1.0(일봉 판독 절). 종목 전환 시 select는 쌍검을 유지한다.

## 운영 (`bot/cycle.py`)
- 구조: Features 하나(공개 스트림) 위에 **방향별 Book**(Strategy·포지션·주문·스탑·일 카운터·clientOid 접두사 `cycL-`/`cycS-`·OS 락). `strat.sides`가 `["long"]`/`["short"]`면 외검, `["long","short"]`면 쌍검 — 쌍검이면 고정·비율 예산(unit·cap·일손실·명목)을 방향 수로 나눈다. 프라이빗 채널은 clientOid 접두사·holdSide·posSide로 Book에 배정된다. `state.json`은 `books.<side>` 아래에 방향별 블록(첫 방향은 호환용으로 최상위에도).
- `params.json` 매초 핫리로드. symbol/sides/mode 변경과 상태형 신호 창 변경은 프로세스 재시작(감시견이 올림) — 단 live에서 어느 book이든 포지션·주문이 있으면 전부 비워질 때까지 보류(`PARAMS_DEFERRED`); 모드가 바뀐 재기동은 이전 모드의 장부(로트·실현손익·손절 횟수·HALT·쿨다운·사이즈)를 전부 버린다(`STATE_DISCARDED`). 신호 키는 파일 전체를 한 스냅샷으로 적용한다(파일에서 지운 키는 기본값으로; 창 변경이 보류 중이면 신호 키 전체가 재기동까지 기다린다). 포지션·평단·스탑·프리셋 id·쿨다운·실현손익·손절 횟수·자본 비례 사이즈는 `logs/state.json`에 남아 재기동해도 이어지고, UTC 날짜가 바뀐 재기동은 일일 HALT를 푼다. Strategy의 연성 상태(거부 횟수 `fail_n`, derisk 래치, 브레이크 근거 `brk_seen`, 되돌림 고점 `peak`, 진행 중 pull·arm)는 남지 않는다 — 재기동 뒤 게이트는 정상값으로, 래치는 다음 틱에 조건이 맞으면 다시 걸리고, 대기 중이던 덜기 주문은 흡수됐다가 pull이 없으므로 취소된다.
- `mode: dry`는 로그와 가상 체결(`signal.sim_match`/`sim_book`, 백테스트와 공용: 가격 관통 — 단 등록 1초 안의 관통은 체결이 아니라 post-only 취소(`CANCEL why=crossed`; live 기록에서 등록 1초 안의 관통은 미체결 4:1) —, 또는 앞 잔량 소진 — 우리 쪽을 치는 공격자 체결과 **호가 스냅샷이 드러내는 취소**(체결로 설명되지 않는 표시량 감소를 레벨 전체에 균등 배분)로 줄어든다 —, 또는 **반대편 최우선 호가가 우리 가격에 닿음** — 등록한 초에 우리 가격의 프린트가 없으면 post-only 취소(live 도착 취소 95건 중 94건이 이것이고 대부분 프린트 없이 호가만 지나갔다), 그 뒤엔 거기 걸린 수량만큼 체결(체결 가능한 호가는 대기 주문과 맞물린다) —, 메이커 0.02%/테이커 0.06%). `mode: live`는 사용자의 "전환해"에만, (심볼, 방향) 배타 소유, `adopt: true`로 기존 포지션·스탑 흡수. Bitget은 시장가를 넘는 post-only를 접수 후 취소한다(체결 없음) — `orders` 채널의 cancelled로 처리된다.
- **live 정합성(거래소가 진실)**: 프라이빗 WS 구독이 확인된 뒤에야 REST 스냅샷(`resync`)을 찍고, 프라이빗 재접속 후와 60초마다 다시 찍는다 — 포지션 수량 vs 로트(불일치가 10초 지속 → HALT: 우리 로트가 없으면 UNOWNED_POSITION, 있으면 EXTERNAL_FILL — 큐에 남은 자체 체결이 반영될 유예), 우리 접두사의 미체결 주문(잊은 건 흡수, 중복은 취소, 거래소에 없는 건 폐기), 포지션 스탑. 포지션이 있는데 거래소 스탑이 없으면 resync와 매초 housekeeping이 즉시 등록한다(fallback = 저장된 스탑, 없으면 평단 − cap; 공개 틱을 기다리지 않음). 취소는 REST 성공 후에도 `orders` 채널/`order_detail`로 종결이 확인될 때까지 추적하고 그 역할의 새 주문을 내지 않는다(거래소가 "없음/이미 완료"라고 답할 때만 즉시 폐기; "cancel" 문자열만으로는 안 됨). 주문 제출이 타임아웃되면 clientOid로 미확정 추적 → 5초 뒤 미체결 조회로 확정, 15초 안에 안 보이면 폐기; 미확정 주문의 취소는 id가 확정된 뒤 실행된다(`CANCEL_DEFERRED`). 시장가 응답이 유실되면 `TAKER_UNCONFIRMED`로 남기고 clientOid 조회로 종결(`TAKER_SETTLED`)될 때까지 새 시장가를 내지 않는다. 프리셋 손절은 WS에서 `planType: "sl"`(포지션 손절은 `psl`), pos_loss 확인 후에만 정리하고 실패 시 10초마다 재시도. 비상 청산과 메이커→테이커 전환은 대기 주문의 취소가 거래소에서 확인된 뒤에만 시장가를 낸다(비상은 취소 실패를 재시도하며 최대 6초 대기; 대기 덜기가 수량을 잠그면 시장가가 일부만 청산됨). 비상 시장가의 응답이 유실되면 clientOid로 조회한 뒤에만 재전송한다. 손절 체결 뒤에도 대기 주문은 취소 확인까지 추적한다. STOP 종료는 취소 확인을 최대 5초 기다리고 남은 주문을 ERROR로 남긴다. `RESUME` 파일은 HALT가 없어도 소비한다(묵은 파일이 나중 HALT를 풀지 않도록). UTC 자정엔 일손실·손절 횟수 초기화와 함께 DAILY_STOPS/DAILY_LOSS HALT가 풀린다(백테스트 동일). params는 시작 시와 핫리로드마다 STRAT/SIG와 파일 전용 숫자 키(daily_loss_limit, *_frac)를 타입·부호까지, 창 길이·분모(vol_hl, swing_s, unit_qty …)는 양수까지 검증한다(시작 시 불량이면 EXIT). 실행 중인 빌드가 모르는 키는 숫자여야 통과하므로, 새 bool 스위치를 params에 먼저 쓸 때는 `true/false`가 아니라 **0/1**로 쓴다(2026-08-30 `derisk_on_against: false` → 구 빌드가 `PARAMS_INVALID`로 파일 전체를 거부, 0으로 고쳐 통과).
- **손절 체결 식별**: 트리거된 플랜 주문의 체결은 clientOid = 플랜의 orderId로 온다(psl·sl 모두, 녹화로 확인). 우리가 내지 않은 close 체결은 그 식별자(현재 스탑·프리셋·알려진 플랜 id)로 손절 체결로 잡는다 — 채널 도착 순서는 믿지 않는다. 모르는 식별자면 algo 채널이 이름을 댈 때까지 최대 15초 보류(`CLOSE_FILL_PENDING`, 그동안 수량 불일치 타이머 정지)한 뒤에야 EXTERNAL_FILL. **보류 동안 그 책은 담지 않는다** — 대기 담기를 즉시 거두고(`ENTRY_CANCEL close fill pending`) 분류될 때까지 PAUSE다: 그 사이 담은 로트를 뒤늦게 손절로 판정된 수량이 LIFO로 지우면 거래소는 새 진입가를, 장부는 옛 진입가를 들게 되고 수량이 같아 불일치 감시도 못 잡는다. 한 손절 주문은 분할 체결돼도 한 번만 센다. pos_loss 제출이 타임아웃되면 대기 플랜을 조회해 이미 있으면 흡수하고(실패로 세지 않음), 새 스탑은 기존 pos_loss가 있으면 흡수할 뿐 중복 등록하지 않는다. 프리셋(loss_plan) id는 state.json에 남고 resync가 다시 찾는다.
- **마진 모드(`strat.margin_mode` "crossed", 2026-09-02)**: 거래소는 마진 모드를 심볼마다 기억한다. 08-30 의 크로스 전환은 TRUMP 한 심볼에만 한 것이라 뒤에 들어온 ZEC·HYPE 는 격리(HYPE 20×)로 돌았고, 격리에선 청산가가 포지션 자신의 것이라 **청산가 가드가 cap 대신 스탑이 됐다**(1유닛 cap −10% 자리에 가드가 −3% 를 놓고 래칫이 그걸 끌고 다녔다; HYPE 손절 2건 −21.6, 감사 7). 그래서 모드도 계약이다: 엔진이 5분마다 계정 모드를 읽어 params 와 다르면 **모든 책이 flat 일 때 바꾼다**(`MARGIN_MODE_SET`, 이어서 그 모드의 레버리지 설정), 포지션이 있으면 시간당 한 번 `MARGIN_MODE_MISMATCH` 알림. preflight 는 불일치를 FAIL 로 잡는다.
- **레버리지(`strat.lever` 10, 2026-09-02)**: 사이즈가 아니다(유닛은 지갑 몫의 명목가) — 포지션이 잠그는 초기 증거금이고, 그래서 담기의 증거금 게이트(`avail ≥ 유닛명목/레버 × 1.2`)가 바구니 전체에 거는 유일한 계좌 단위 브레이크의 세기다. 책마다 다르면 브레이크가 종목마다 다르게 걸리므로 엔진이 5분마다의 레버리지 조회에서 params 값과 다르고 **모든 책이 flat(포지션·대기 주문 없음)이면** 거래소에 맞춘다(`LEVER_SET`; 크로스는 심볼당 하나, 격리는 방향별). 새 심볼은 거래소 기본값으로 온다(HYPEUSDT 가 20× 로 들어왔다). 0이면 손대지 않는다. 변동폭에 따른 조절은 레버리지가 아니라 사이즈의 문제다(NEXT 8 σ 정규화, `select.sigma_norm`).
- **피드**: 프라이빗·퍼블릭 WS는 모든 구독 ACK가 온 뒤에야 connected(10초 내 미응답 → 재접속, 구독 거부 → EXIT). 퍼블릭 피드가 30초 이상 조용하거나 프라이빗이 끊기면 대기 담기 주문을 취소한다(`ENTRY_CANCEL`; TTL·재가속 취소는 틱이 있어야 돌기 때문). dry 모드는 account 외의 프라이빗 이벤트를 무시한다(같은 심볼의 수동 체결·스탑이 시뮬레이션에 섞이지 않게).
- 파일: `logs/state.json`(5초·변경 시), `logs/events.jsonl`(전부), `logs/alerts.jsonl`(HALT·STOP_HIT·DAILY_LOSS·WS_DOWN·EXTERNAL_FILL·STOP_FAILED·STOP_MODIFY_FAIL·STOP_THROUGH·EMERGENCY_CLOSE·MARGIN_LOCKED·MARGIN_MODE_MISMATCH·REGIME_CHANGE·SIDE_HINT·PARAMS_INVALID·PARAMS_DEFERRED·STATE_DISCARDED·EMERGENCY_CANCEL_UNCONFIRMED·TAKER_UNCONFIRMED·ERROR·EXIT — 비상 청산 경로 두 개는 BOOT의 "즉시 상태 확인" 대상이라 알림이고, `STOP_LIQ_GUARD`는 1~2유닛의 정상 상태라 events.jsonl에만 남는다). 제어 파일: `STOP`(우리 주문 취소 후 종료), `PAUSE`(담기만 중단), `RESUME`(HALT 해제).
- 프로세스: `python -m bot.supervise record` / `python -m bot.supervise cycle`. 녹화는 `data/ws/`. 재생·튜닝: `python -m bot.replay data/ws/pub-*.jsonl(.gz)`.

## 도구: 스캐너·백테스트·튜너 (모두 리포트 우선)
- `python -m bot.scan [--days 3 --min-vol 50e6 --top 15 --n-books 4 --json --sym A,B]` — 종목 표(공개 REST만). 점수는 **엔진의 돈 단위**다: `edge%/h = 시도/h × (평균 결과% − fee% − imp%)` — 전부 유닛 명목가의 %라 통행료와 기회가 같은 단위로 빠진다. 열 값은 24h 창 3개(최근순)의 **중앙값**. 순위는 `edge` 내림차순(자격 통과·`entry` 통과가 앞).
  - **시도(`trials`, 세 장벽)**: `signal.candle_rule`(live `1m` 규칙 그대로)이 발화할 때마다 그 종가에 시도 하나를 열고 — DIP_SLOWING은 롱, POP_STALLING은 숏(쌍검) — 먼저 닿는 것으로 닫는다: `+win_pct`(롱은 고가), `−loss_pct`(저가), `hold_min`분 만료(만료면 그 시점 수익률을 [−loss, +win]로 클립; 실측 만료 비중 0~8%). **한 봉이 양쪽 장벽을 다 담으면 불리한 쪽으로 읽는다**(봉 안의 순서는 관측 불가). 시도를 열려면 앞으로 `hold_min`봉이 남아 있어야 하고, 비율의 분모(`bars`)도 그 봉만 센다. **체결을 하나도 흉내내지 않는다** — 옛 캔들 프록시가 틱 엔진의 순위를 4/4로 뒤집은 원인이 체결·수수료 모델이었고(덜기를 전부 테이커로 매기면 TRUMP 08-30이 +3.54 → −0.45), 그래서 프록시는 2026-09-01에 **코드에서 삭제했다 — 같은 형태로 다시 만들지 마라**(체결 모델을 `bot.recon`으로 보정한 프록시는 다른 물건이고, 되살리는 조건은 NEXT 6에 있다).
  - **엔진 기하(win_pct / loss_pct / fee_pct)** — 장벽이자 배당이고, **실거래 장부에서 잰 값**이다(gross 기준; `bot.cycles` 마지막 줄). select 은 스캔마다 `bot.cycles.geometry()`로 **다시 잰다**(완결 사이클 ≥ 200; 그 전엔 `scan.EDGE` 상수 0.360/0.569/0.048; `params.select.edge`가 둘 다 덮는다) — 2026-09-02 실측은 282 사이클에 0.345 / 0.698 / 0.047 이라 손익분기 승률이 상수 시절의 0.665 에서 **0.714** 로 올라 있었다(실측 승률 0.709 → 사이클당 −0.005%, 즉 손익분기). 손익분기 승률 = `(loss+fee+imp)/(win+loss)`; imp% 0.009마다 필요 승률 1%p. 표본 대부분이 TRUMPUSDT라 **심볼 불변 가정은 거의 검증되지 않았다**(NEXT 6). `hold_min` 120은 고정.
  - **`imp%` = σ_daily × √(유닛 명목가 / 24h 거래대금)**, 적합 상수 없음. 유닛은 **진짜 북당·방향당** 값(`unit_frac × equity ÷ n_books ÷ 방향수`) — 옛 코드는 `wallet_frac`을 안 나눠 √2 과대였다. 제곱근 법칙은 기관 참여율용이라 상한으로 읽는다(보정은 틱 백테스트, NEXT 6).
  - **하드 자격 상실(플래그 — 후보에서 빠지고, 들고 있으면 wind-down)**: `imp% ≥ fee%`, 거래대금 < `--min-vol`(**50M, 휴리스틱 백스톱**: 파생 게이트인 `imp% ≥ fee%`는 이 자본에서 안 걸린다 — 우리 유닛이 ADV의 ~5ppm인데 그 게이트는 ~50ppm에서 걸린다), 틱% > 0.05, 스프레드 > 10bp, |펀딩| ≥ 0.1%/8h, 며칠의 펌프 모양(거래량 집중 시간이 가격을 4% 이상 움직인 날이 2일 이상), 펌프(한 창 +25% 또는 창 합 +40% — 가는 길이 아무리 위아래로 흔들려도; 2026-08-30 PROMUSDT +54%/일이 반등 기반 규칙을 통과했던 구멍), 최근 창 ER ≥ 0.35, **`slow`: 시도/h < `select.min_trials_h`(1.3)** — 30일에 캠페인 300이 안 쌓이는 종목은 live 로 판정할 수 없고 판정 못 하는 종목은 자격이 없다(CONCEPT; 2026-09-02 XAU 0.25/h). 표에는 `sigma_d`(일 σ%)와 `cluster`(15분 수익률 상관 ≥ `corr_th` 0.5 로 묶은 드라이버 군집, 시드 심볼이 id), **`tr_by_h`**(UTC 시간대별 시도/h 24개 — 토큰화 주식·원자재의 세션 모양; XAG 는 UTC 02~18 에만 시도가 있다, NEXT 8 시간대 배분의 데이터)도 실린다. **하루 급락은 플래그가 아니다**(급락하는 놈이 순환매 천국인 경우가 많다; 그 손실은 스탑의 몫) — 비대칭이 의도다: 작전 코인은 위로 가는 놈이다.
  - **소프트 게이트 `entry`** = 시도 하나가 통행료를 내는가(`out% > fee% + imp%`). **담기만 막고 절대 나가게 하지 않는다.** 근거: 추정기가 이 엔진보다 비관적인 것이 측정돼 있다 — TRUMPUSDT 자기 3일(09-01/08-31/08-30)에서 `p_up` 0.59 / 0.62 / 0.59인데 같은 날 실거래 승률은 0.65 / 0.72 / 0.64이고 **날짜별 변동폭이 1/3이다**(ZECUSDT는 `p_up` 0.59 vs 실거래 0.867, n=15). 원인은 메커니즘이다: 엔진 출구는 고정 장벽이 아니라 `gate_relax`로 내려오는 게이트라 아슬아슬한 시도를 작은 이익으로 바꾼다(그래서 승률 0.70에 평균 gross 이익 0.360 / 평균 gross 손실 0.569). **그래서 점수는 자격 통과자를 줄 세우는 데만 쓴다.** `edge_sd`(창별 표준편차)가 대개 `edge`보다 크다 — 순위 자체가 잡음 안에 있고, 그래서 한 종목이 아니라 N종목을 든다.
  - `rank(always=…)`는 들고 있는 심볼을 거래대금 게이트 아래에서도 **측정**하지만 **자격을 면제하지 않는다** — 측정은 자격이 아니다. 2026-09-01 09:55 TRUMPUSDT가 게이트 아래로 떨어졌을 때 옛 코드는 면제했고, 그 뒤 13시간을 −0.159%/사이클로 돌았다.
  - **보고만 하고 순위에 안 쓰는 것**: `concept = legs/h × leg% × bounce × (1 − ER)`과 그 부품(비교를 이어가기 위해 남긴다). 19종목 횡단면에서 1−ER 0.95~1.00, bounce 0.73~1.00, `leg% ≈ 5.3 × atr%` — 사실상 **빈도 × 변동성**이고 변동성은 imp%를 같이 올린다. **왜 실패했는지의 숫자**: TRUMP 거래대금 붕괴(53M → 25M, 09-01 09:55) 전후로 평균 gross 이익 0.393 → 0.284, 평균 gross 손실 0.511 → 0.718, 사이클/h 4.21 → 2.15, 실현 엣지 +0.100 → −0.159%/사이클인데 concept은 1위 그대로였다. 같은 시간대에 ZEC이 +0.115 / TRUMP가 −0.073이었으니 시장이 아니라 종목이었다(n=15 vs 14 — 비교의 종류가 맞을 뿐 크기는 아직 작다). 그리고 우리가 사이클마다 먹는 폭은 **우리 게이트**가 정한다 — 중앙값 gross TRUMP 0.233%(209 사이클) vs ZEC 0.295%(15)인데 `leg%`는 35% 차이였다. **다리 크기로 종목을 고르는 것은 손익에서 거의 상쇄되는 항으로 고르는 것이다.**
- `python -m bot.supervise select` (`bot/select.py`; `--once --dry`는 판정만 하고 `logs/scan.json`만 쓴다) — **집중된 바구니 관리(2026-09-02)**. `every_h`(4)시간마다 스캔하고 `params.json["books"]`를 목표 바구니로 옮긴다: **본 책 `select.n`개 + 프로브 책 1개**, 심볼마다 엔진 하나(`bot.supervise cycle`).
  - **비중(`wallet_frac`)**: 본 책은 본 풀(1 − `probe` 0.1)을 **슬롯 수 n으로 나눈 균등**이 기본이고, live 장부에서 1위가 2위를 `sigma`(2.0) 표준오차 이상 앞서면(둘 다 `min_cycles`(60) 이상; 근거 = 최근 `evidence_days`(5)일 완결 사이클의 사이클당 순손익% × 시간당 사이클 = **시간당 엣지**, 스캐너 점수와 같은 단위) 1위가 풀의 `lead`(0.5)를 갖고 나머지가 n−1 슬롯으로 나눈다. 프로브는 `probe`. 합은 언제나 ≤ 1(빈 슬롯은 돈을 놀린다). `sigma_norm`(0) = 1이면 본 책 비중 × 1/σ_daily로 재정규화(돈 한도의 σ 거리 균등; NEXT 8의 짝 표가 근거를 주기 전엔 끈다). 비중 변경은 엔진이 flat일 때 스스로 반영한다(resize).
  - **프로브**: 바구니가 찼을 때 자격·`entry` 통과 1위 후보를 `confirm`(2)회 연속 뒤 지갑의 `probe`로 굴린다(같은 엔진, 같은 규칙; `books[sym].probe = 1`). `min_cycles` 사이클(또는 `probe_days`(5)일 + 그 절반)이 쌓이면 **가장 약한 측정된 본 책**과 시간당 엣지를 비교: `sigma` SE 이상 앞서면 그 책은 wind-down, 프로브는 그 책이 빠진 뒤 본 책이 된다(`BOOK_PROMOTE` → `BOOK_MAIN`); 아니면 프로브가 wind-down(`BOOK_PROBE_END`)하고 `probe_cooldown_d`(7)일 쉰 뒤 다음 후보가 프로브한다. 선정을 추정에서 실험으로 옮긴 것: 다음 종목의 live 근거는 다음 종목을 굴려야만 생긴다.
  - **퇴출**: (a) 스캔의 하드 플래그(순위 무관), (b) **자기 장부** — `min_cycles` 이상에서 사이클당 순손익이 0 아래로 `sigma` SE 이상, `confirm`회 연속 스캔(`BOOK_EVICT`, 쿨다운 뒤 후보 복귀), (c) 프로브에 진 것. **순위 때문에 갈아치우지 않는다**는 그대로다 — (b)·(c)는 순위가 아니라 그 종목 자신의 실거래다.
  - **들어오기**: 자격 통과 + `entry` 통과 후보를 `edge` 순으로 빈 본 슬롯에, `confirm`회 연속, 하루 `max_per_day`(2)회(프로브 개시 포함), `exclude`(BTC)·쿨다운 중 심볼 제외, 그리고 **드라이버 군집당 본 책 `per_cluster`(2)개까지**(군집 = 스캔의 15분 수익률 상관 ≥ `corr_th` 0.5, 시드 심볼이 군집 id; 크립토 4개는 급락일에 한 베팅이다).
  - **단일 종목으로 가는 조건**: `bot.pair`가 날마다 추정기 1위(`*`)와 BASKET 평균의 시간당 엣지를 나란히 낸다. 1위가 평균을 2주 연속 이기면 `select.n`을 줄인다 — 그 전엔 집중은 비중 기울임까지다.
  - 오프라인 랭커로 갈아타지 않는 이유(그대로): 이 표본 크기에서 검증된 오프라인 랭커가 없다(프록시 4/4 역전, 틱 백테스트 잡음이 재려는 효과의 3배, 새 점수의 `p_up`도 live와 어긋난다 — 09-01 TRUMP 엔 비관, 09-02 ETH·HYPE 엔 낙관). 순위 기반 교체가 곧 옛 `ratio` 1.5배 허들이었고, 그 랭킹에는 비용 항이 아예 없었다. 점수는 빈 슬롯과 프로브 슬롯을 채우는 순서일 뿐이다.
  - **wind-down**: 플래그 붙은 책에 `books[sym].wind_down = 1`(그 심볼만의 PAUSE — 담기 중단, 덜기·스탑 유지) → 그 엔진이 flat을 보고하면 키를 지운다 → 엔진은 스스로 종료한다(`cycle.py`: 못박힌 심볼이 books에서 사라지면 계약 변경, 포지션이 있으면 flat까지 보류 — **보류 중엔 지갑 몫을 유지하고 wind_down 으로 고정된다**; 빠진 심볼은 `strat_for`가 공통 기본값(wallet_frac 1.0, wind_down 없음)으로 떨어지므로 2026-09-02 전엔 보류 중 담기가 되살아날 수 있었다). flat 판정은 **스캔이 끝난 뒤 다시 읽은** state 로 한다(스캔은 60~80초, flat 창은 60초라 스캔 전 state 로는 영원히 flat 이 아니었고 BOOK_DROP 이 한 번도 안 났다 — 2026-09-02 수정). 다음 스캔에서 플래그가 사라지면 wind_down 을 지운다(`BOOK_RESUME`; ER 은 스캔마다 재판정이다). 시장가로 던지지 않는다(CONCEPT). **`books`는 절대 비지 않는다** — 마지막 책은 wind_down 상태로 남는다(비면 `ws.portfolio()`가 `strat.symbol`로 폴백해 지갑 **전액** 사이징이 된다).
  - 스캔 표에 없는 심볼은 무조건 keep — 부재는 근거가 아니다.
  - **사람이 `books`를 손으로 고쳐도 된다**(예외적 개입). select은 다음 스캔에 그 상태를 그대로 현직으로 받아들이고 `wallet_frac`만 1/n으로 다시 정규화한다 — 되돌리지 않는다. 손으로 뺄 심볼은 그 엔진이 flat일 때만 빼라(엔진은 books에서 사라지면 스스로 종료하지만, 포지션이 있으면 flat까지 보류한 채 계속 담는다 — `wind_down`을 켜지 않는 한).
  - 같이 쓰는 것: `strat.symbol`(들고 있는 것 중 1위 — `bot.trade`와 못박히지 않은 엔진의 기본값), `strat.side`(그 심볼의 1H 구조, 기록용; 쌍검 `sides`는 유지), `record`(모든 책 + 하루 안에 state 를 쓴 엔진의 심볼(막 빠진 책의 테이프) + 상위 `record_top`(5) 후보 + `record_extra` 워치리스트(플래그 무관, 녹화만) + BTC 캔들). 나머지 params 키는 사람이 쓴다.
  - 이벤트 `SELECT`(매 스캔 판정, 그 스캔이 쓴 기하 `edge` 포함), 알림 `BOOK_ADD` / `BOOK_WIND_DOWN` / `BOOK_RESUME` / `BOOK_DROP`. 상태 `logs/select-state.json`(연속 통과 수, 오늘 개시 수). **`logs/scan-history.jsonl`** — 매 스캔의 행 전체(p_up·trials_h·edge·impact…)를 덧붙인다(2026-09-02; `scan.json`은 덮어쓰므로 심볼×날짜의 추정기-vs-실적 짝 비교(NEXT 6·8)의 추정기 쪽 원천이 이것뿐이다).
  - **아직 없는 것**: 시간대별 배분(토큰화 주식·원자재는 거래 시간이 몰리는데 24h 창의 `tr/h`가 뭉갠다 — NEXT 8), σ 정규화의 부호(스위치만 있다).
- `python -m bot.backtest FILE... [--sym S] [--equity E | --fixed]` — 녹화를 초 단위로 압축(`data/cache/`)해 Features + Strategy + dry 체결 모델을 그대로 돌린다. 출력: 실현손익·미실현·사이클(로트가 다 팔린 횟수)·추가·손절·최대낙폭·시장 체류율·그 실행의 사이즈. **사이즈는 live 와 같다**(2026-09-02): strat 은 `ws.strat_for(params, sym)`(그 책의 wallet_frac·sides; books 밖 심볼은 슬롯 하나 1/`select.n`), 수량 step 은 그 심볼의 계약(`data/cache/contract-*.json`, `--qstep` 으로 덮음), 자본 비례 사이즈는 `--equity`(기본 = 최근 state 의 equity)로 첫 mid 에서 고정 유닛·cap·한도로 굳힌다(오프라인엔 resize 가 없다). `--fixed`는 파일의 unit_qty·cap_usdt·daily_loss_limit·max_notional 그대로 — 자본과 무관한 **기준 수치는 이것으로 잰다**. 그 전엔 `--sym` 을 바꿔도 TRUMP 의 유닛 70·step 0.1 로 돌아 ZEC 유닛이 $57k 가 됐다. 초 순서: 이전 초를 그 초의 호가·흐름으로 닫고 → 결정 → 그 초의 체결을 기존 주문에 적용 → 그 초의 호가(앞 잔량에서 취소분을 뺀다, `sim_book`). 한 체결은 모든 book의 같은 가격 주문이 등록 순서로 나눠 먹는다(`signal.sim_match`, dry 모드와 공용 — 변경 시 둘 다). **체결 모델(2026-09-02 보정, 세 규칙)**: (1) 등록한 초의 관통, 그리고 반대편 최우선 호가의 도달(그 초에 우리 가격의 프린트가 없을 때)은 post-only 취소(`CANCEL crossed`)이지 체결이 아니다, (2) 앞 잔량은 우리 쪽을 치는 프린트뿐 아니라 매초 호가가 드러내는 취소로도 준다, (3) 등록한 초가 지난 뒤 반대편 호가가 우리 가격에 닿으면 거기 걸린 수량만큼 체결이다. 근거: live 메이커 861건을 녹화로 재생하면 프린트만 보는 옛 모델은 실제 체결된 주문의 45~50%를 생존 시간 안에 못 채웠고(취소 인식 8~14%, 그 잔여의 1/3 은 반대편 호가 도달이 설명), 거래 피드는 완전하며(프린트 합 = 1분봉 거래량 1.00), prv 녹화의 orders 채널로 보면 접수는 송신 ~40ms 뒤, 거래소 도착 취소 95건(11%)은 접수 62ms 뒤이며 94건이 1초 안의 반대편 호가 도달과 일치하고, "1초 뒤 관통인데 미체결" 36건은 전부 엔진이 먼저 ms 단위로 취소한 주문이었다. 남은 한계는 1초 해상도다: 첫 1초 안의 반대편 호가 도달은 live 에서 취소 94 : 체결 84 로 갈리는데 그 차이는 접수(~40ms) 전후이고, 모델은 그 초에 우리 가격의 프린트가 있었는지로만 가른다. `adds`는 부분 체결이 아니라 **채워진 주문 수**다(recon 의 live clientOid 와 같은 단위). 이전 구현은 다음 초 끝 호가로 닫는 1초 look-ahead가 있었고 그때의 수치는 무효다. **시딩**: 시작 전에 live와 같은 이력을 넣는다 — 파일 시작 시각 이전의 1분봉 1000·15분봉 200·일봉 80을 공개 REST(`history-candles`)로 한 번 받아 `data/cache/seed-<sym>-<start>.json`에 둔다. 녹화의 1분봉 스냅샷(500개)은 녹화기의 첫 파일에만 있고 그 뒤 파일은 무이력이라, 시딩 없이는 ATR15·15m/1H 구조 손절이 몇 시간 비어 live와 다른 엔진을 돌린다(2026-08-29 저녁 테이프 무시딩: step 0.5%·구조 없음·derisk 래치 오발). 네트워크가 없으면 녹화만으로 돌고 stderr에 경고; 일봉 추세는 live처럼 매시간 갱신하지 않는다. **기준 테이프 — `python -m bot.backtest FILE... --fixed`(TRUMPUSDT, 파일의 유닛 70·cap 20·명목 900·일손실 40, 쌍검), 2026-09-02 10:19 이후의 live 설정(체결 모델 세 규칙 · 코어 게이트 바닥 = 왕복 수수료 · `rg_leg_on` 1 · `derisk_pct` 0 · `retrace_frac` 0.2) 기준 수치**: 04–06(캐스케이드 날) **+1.33**(19 사이클, 손절 1, DD 8.9) · 07–10(두 방향 장) **+4.67**(13, 0, 3.6) · 27h(08-29 04 → 08-30 06 UTC) **+15.09**(103, 1, 8.9) · 08-31 24h **+7.38**(79, 0, 4.6). 같은 날 아침(옛 체결 모델·코어 바닥 0·레그 판독 없음·derisk 켬·retrace 0.33)은 −5.83 / +1.99 / −0.55 / +1.74 였고, 그 사이 단계별 수치는 NEXT 1·3·5 의 표에 있다. 이 수치는 params 를 그대로 읽으므로 **params 가 바뀌면 같이 바뀐다** — 비교는 같은 params 에서만. 이 절의 다른 수치(derisk 표, retrace 격자, 쌍검 비교)는 보정 전 모델의 것이라 절대치가 아니라 관계로 읽는다. 단방향 수치(롱 −9.1 / 숏 풀 +2.5 등)는 2026-08-30 측정이라 쌍검 채택 근거로만 읽는다. 백테스트는 청산가 가드·펀딩이 없다; `--fixed`의 cap/명목 = 10.6%/n 은 live(cap_frac 0.15/unit_frac 1.5 = 10%/n)와 같다.** 2026-08-29 밤 이전 코드(브레이크 담기 거부·flat gap 앵커·먼지 꼬리)는 같은 시딩에서 04–06 롱 −7.1 / 07–10 롱 −0.0 — 캐스케이드 날의 차이는 먼지 0.27계약이 gap_rebuy 앵커를 살려 14:30·14:48 재진입을 막은 우연이 컸고, 두 방향 장의 차이는 흘려보내던 감속을 사이클로 만든 결과다. 수치로 메커니즘을 되돌리지 않는다(CONCEPT: 결과를 보고 규칙을 고치지 않는다) — 원웨이 날의 손실은 레짐 브레이커·구조 손절·cap의 몫이다.
- **평단/방향 개념의 측정**(2026-08-30, 사용자 개념 "순환매는 평단을 손해 없이 조절하는 매매, 돈은 그 위에서 방향이 낸다" — 규칙이 아니라 증거): (a) `bot.replay`의 각 정체 신호에 **hold vs sell** — 정체가 팔았을 쪽(POP_STALLING이면 롱)을 0.5/1.5 ATR 트레일 스탑으로 30분까지 들었을 때의 손익(양수면 코어를 정체에서 판 것보다 든 게 나았다), 야간에 `side_hint_1h`/`side_hint_15m`으로 분할; (b) `python -m bot.capture --day YYYYMMDD [--sym SYM]` — live 장부(events.jsonl)를 그날 1분봉과 겹쳐 **분 단위 상승/하락 이동 중 보유 중이었던 비율**과 ≥3 ATR 다리 단위의 같은 비율(롱이 상승 다리보다 하락 다리를 더 많이 들고 있으면 사이클 크럼을 방향의 돈으로 사는 중), 백테스트 metrics의 `capture`도 같은 정의. **books의 심볼마다·방향마다 한 줄**이다 — 한 파일에 섞인 이벤트를 심볼·방향으로 가르지 않으면 다른 책의 보유가 이 책의 보유가 된다(2026-09-01: 가르기 전 TRUMP·ZEC이 똑같이 in_mkt 0.56, 가른 뒤 0.57 / 0.28). 심볼 태그가 없던 이벤트(2026-09-01 이전)는 직전 `START`의 엔진 것이다. 첫 날(2026-08-29): 정체 뒤 홀드는 평균 −0.04%(트레일 0.5 ATR, 32% 승) — 그날은 정체에서 파는 게 맞았다; live 상승 다리 보유 27% vs 하락 다리 35%(시장 체류 0.36). 사이클 스케일 FAVOR(코어 보유+트레일)는 이 두 표가 며칠 갈릴 때만.
- `python -m bot.backtest FILE... --follow 15m|1h|brk|regime` — **방향 자동화 반사실**(2026-08-30 사용자 질문 "15m 자동화였으면"): 두 책이 있지만 활성 쪽만 매매, 시작은 `strat.side`, 전환은 플랫에서만 15m/1H 구조 힌트의 쪽으로(None은 유지), `brk` = 마지막 볼륨 브레이크의 쪽으로, `regime` = 롱 책의 레짐 라벨(AGAINST → 숏, FAVOR → 롱; 같은 잠재변수 "사이클 지평의 시장 방향"의 네 추정기); `follow={flips, share}`와 FLIP 이벤트. 야간 리포트 sides 절에 15m·brk 포함. 27h 테이프(08-29 04 → 08-30 06 UTC) 결과: 롱 고정 −13.30 / 숏 고정 +4.97 / 쌍검 −7.03 / **follow 15m −12.63(전환 1회, 08-29 22:24 UTC 숏) / 1H −13.30(전환 0) / brk −14.01(전환 7회)**. 읽기: 롱·숏 고정의 차이는 거의 전부 급락 2시간(04–06: 롱 −16.3 vs 숏 +0.9)이고 그 뒤 24시간 흘러내림에서는 롱 +3.0 / 숏 +4.1로 비슷하다 — 구조 읽기는 스윙 두 개가 완성돼야 읽히므로 급락 중엔 None(15m)이거나 롱(1H)이고, 브레이크 추종은 급락의 첫 손절 뒤에야 뒤집혀 손실을 이미 안은 채 이후 되돌림 브레이크마다 뒤집힌다. **방향 규칙의 가치는 드리프트가 아니라 꼬리에 있고, 꼬리 전에 서 있는 쪽은 규칙이 아니라 콜이다.** 방향 세션은 이 표를 기준선으로 한다.
- `python -m bot.sweeps FILE... [--quiet --day YYYYMMDD]` — **스탑헌팅 증거표**(2026-08-30 사용자 질문 "스탑헌팅도 녹여낼 수 있나"): 엔진이 손절을 고르는 바로 그 레벨(15분·1H 피벗, 15분 이상 확정된 것만)을 가격이 쓸고 내려갔다 되찾는 sweep을 초 단위로 잡아 — 시간당 횟수, 되찾음 비율, 깊이(ATR15·%), 되찾은 sweep 중 버퍼(0.3/0.5/0.8/1.2 ATR15)보다 깊은 비율(= 우리 스탑이 헌팅당했을 비율), 되찾은 뒤/못 되찾은 뒤 15분 성적. 두 얼굴을 한 표로: (방어) `stop_buffer_atr`(튜너 격자에 있음)의 데이터 근거와 "넓은 거래소 스탑 + 확인된 소프트 스탑" 구조의 가치, (공격) sweep & reclaim을 세 번째 검출기로 쓸 가치. 야간 리포트에 누적. 메커니즘은 며칠 표가 갈릴 때 속도 모델 세션에서.
- `python -m bot.legs FILE... [--atr 3.0 --quiet --day YYYYMMDD]` — 속도 모델의 증거표. 초 단위 mid에서 ≥ `dip_min_atr`×ATR 지그재그로 실제 다리의 바닥·꼭대기를 찾고, 다리마다 깊이·지속·속도·다리 안의 `v` 극값, 그리고 검출기별(엔진 `v`·`1m`·`s8` 신호 — `any`는 shadow 를 뺀 Strategy 가 보는 신호 —, 오프라인 후보 지평 속도 c8/c30/c60/c120 = h초 이동/ATR이 다리 최고 속도의 30% 이하로 죽는 시점; c8 은 엔진 s8 과 같은 규칙을 확정 피벗 기준으로 잰 것이라 둘의 차이가 구현 검증이다) 첫 발화의 **극점 대비 지연(초)과 거리(%, step 단위)**를 낸다. 거리가 행동 가능성을 정한다(담기는 직전 매수 −step 아래여야 하므로 1% 눌림을 바닥 +0.6%에서 잡으면 사다리가 안 나온다; "actionable" = 극점에서 반 step 이내). 야간 리포트에 그날 표가 누적된다. 속도 모델 변경은 이 표(≥ 며칠)로 설계하고 walk-forward로 채택한다 — 하나의 눌림에 맞추지 않는다.
- **포트폴리오(여러 심볼 동시 실매매)**: `params.json`의 `books = {심볼: {wallet_frac, wind_down, sides…}}`가 바구니이고 심볼마다 엔진 하나다. **`python -m bot.supervise cycle`이 books를 읽어 심볼마다 자식 엔진 하나를 유지한다**(로그 `logs/cycle-SYMBOL.log`; books가 없으면 예전처럼 못박히지 않은 엔진 하나). select가 더한 심볼은 엔진이 뜨고, 뺀 심볼은 재기동하지 않는다 — 그 엔진은 스스로 종료하고(books에서 사라진 것이 계약 변경), select은 flat인 책만 뺀다. 손으로 하나만 띄우려면 `cycle:SYMBOL`을 쓰되 **그 심볼이 `books` 안에 있어야 한다** — books 밖 심볼로 못박힌 엔진은 스스로 종료한다(그런 엔진은 `wallet_frac` 기본값 1.0으로 지갑 전액을 쓰고, `OLD_POSITION` halt의 대상이다). 엔진의 strat은 `ws.strat_for(p, sym)` = 공통 `strat` 위에 `books[sym]`을 덮은 것 — **규칙은 공통이고 심볼마다 다른 것은 지갑 몫·방향·wind_down뿐이다**(심볼마다 규칙을 따로 두면 포트폴리오가 아니라 다른 전략이다). 상태는 엔진마다 `logs/state-<SYMBOL>.json`이고 `ws.load_states()`가 모아 읽는다(하나도 없으면 옛 `state.json`으로 1회 폴백). `wallet_frac`(select이 **1/`select.n`**으로 쓴다, 0 금지)이 `resize()`의 지갑을 나눈다 — 안 나누면 두 엔진이 각자 계좌 전액으로 사이징해 노출이 심볼 수만큼 배가 된다. **불변식은 합 ≤ 1이다**(`1/len(books)`가 아니라 `1/n`이라 슬롯이 비면 합이 1보다 작다 — 자본을 덜 쓸 뿐이라 안전하다; `preflight`는 초과만 FAIL 로 잡는다). `PULL_TRIM`에는 `mode`(발효 중인 게이트: derisk 는 코어 컷일 때만 / favor / retrace / normal)·**`path`(발화 경로: stall/retrace)**·`lot`(코어 컷이면 "core")이 실리고 그 체결 `FILL`에도 `lot`이 붙는다 — 2026-09-02 이전 로그는 derisk 근거만 켜져 있으면 정상 유닛 매도까지 `mode=derisk`로 찍었다(108건 중 10건). 이벤트에 `symbol`이 붙는다(2026-09-01 17:30 이전 이벤트엔 없고 소급 불가). **`bot.cycles`는 태그 없는 체결을 직전 `START` 이벤트의 심볼에 귀속하고 로트 책을 `(심볼, 방향)`으로 가른다** — `START`는 전 기간 symbol을 담고 그때는 엔진이 하나였다. 이 귀속이 없으면 `--symbol ZECUSDT`가 TRUMPUSDT 장부를 통째로 받는다(2026-09-01에 실제로 그랬다). `OLD_POSITION` halt는 "포트폴리오 **밖** 심볼의 state에 live 물량"으로 일반화. **`books`가 없으면 동작은 단일 심볼 그대로다.** 배분은 균등(1/n)이고 성과 비례로 갈지는 심볼별 live 실적이 쌓인 뒤에(NEXT 8).
- `python -m bot.recon [--day YYYYMMDD --min-hours 2 --sym SYM]` — **백테스트 캘리브레이션**(읽기 전용; 야간 리포트 항목). books의 심볼마다 따로 대조한다: 경계 이벤트·`SIZING`·체결·수량 step(`START`가 기록한 값)을 전부 그 심볼 것만 쓰고, **그 엔진이 돌고 있던 시간대만** 구간이 된다(엔진이 아직 없던 시간대는 경계 이벤트도 없어 깨끗해 보이지만 장부가 0이라 오차가 통째로 가짜다). 하루를 통째로 비교하면 안 된다 — 재기동·HALT·파라미터 변경·전환·인수가 낀 구간은 백테스트가 재현할 수 없으므로, 그런 경계 이벤트가 하나도 없는 연속 테이프 시간대만 골라 그 구간의 실제 `SIZING` 값(방향 수를 곱해 파일 단위로 되돌림)으로 돌린 백테스트를 실매매 장부와 대조한다. TRUMP 11구간(08-29~09-01, 33시간), 2026-09-02 체결 모델 보정 뒤: 구간 오차 평균 **+0.30, sd 1.30**, 부호 7:4 — 보정 전 −0.03 / 1.2 와 같은 크기다. 즉 **창 단위 손익 오차는 체결 모델로 줄지 않는다**: 주문 단위의 체결 예측은 크게 좋아졌지만(옛 모델이 못 채운 실제 체결 45~50% → 8~14%) 한 체결이 어긋나면 그 뒤 로트·게이트가 전부 갈라지는 경로 발산이 창 오차의 바닥이다. 남은 계통 차이: 덜기 주문 live 98 vs bt 사이클 71(보정 전 61; 단위가 달라 느슨한 비교), 담기 주문 74 vs 87(+18%; 반대편 호가 규칙 전 +28% — 첫 1초 안의 호가 도달을 접수 전후로 못 가르는 1초 해상도의 한계). 무작위 오차라 종목 간 비교에서 상쇄되지 않는다(시간당 sd ≈ 0.4 vs 재려던 TRUMP−ZEC 차이 0.138/시간). **이 파일의 backtest 기반 수치는 전부 이 오차를 진다** — 창당 1.5 미만의 차이는 판정 근거로 쓰지 않는다(NEXT 측정 규약).
- `python -m bot.cycles [--from "YYYY-MM-DD HH:MM" --side long|short --top N --csv PATH]` — **사이클 단위 실적표**(`logs/events.jsonl`만 읽는 읽기 전용; 테이프가 아니라 live 장부가 대상이다). 담기 clientOid 하나 = 로트 하나로 묶고 덜기를 LIFO로 소진해 로트가 다 팔린 순간을 한 사이클로 확정한다(엔진 `pos["lots"]`와 같은 규칙). 사이클마다 진입·청산가, 보유시간, 총손익, 수수료, 순손익, 청산사유(trim/stop), 진입 당시 로트 깊이. 요약에 승률·중앙 순손익·중앙 보유시간·수수료 총액과 상위 10/25/50%가 이익 합에서 차지하는 비중. `STOP_HIT`에는 fee 필드가 없어 수수료는 이벤트의 `pnl`에서 역산한다. 검산: 방향별 순손익 합 = 엔진 per-fill `pnl` 합(미완결 로트의 진입 수수료만큼 차이), "못 붙인 청산 수량"이 0이 아니면 재구성이 장부와 어긋난 것이다.
- `python -m bot.slip [--day YYYYMMDD --from "…" --sym S]` — **체결 비용표**(읽기 전용, 야간 절): FILL 을 clientOid 로 PLACE(메이커)·TAKER(시장가)의 도착 `mid`에 잇고, 심볼×메이커/테이커×역할마다 n, 비용(체결가 − 도착 mid, 거래 방향 부호, bp; 메이커는 반스프레드만큼 음수), 드리프트(체결 시점 mid − 도착 mid: 메이커의 역선택·테이커의 임팩트), 달러 비용·수수료. NEXT 6 5항의 직접 관측(`imp% = σ√(유닛/ADV)` 대신) — mid 는 2026-09-02 빌드부터 기록되므로 그 전 체결은 "without" 으로 센다.
- `python -m bot.pair [--day YYYYMMDD --sym S]` — **추정기 대 실적 짝 표**(읽기 전용, 야간 절): `logs/scan-history.jsonl` 의 그날 스캔 중앙값(p_up·시도/h·edge·imp%·entry)과 `bot.cycles` 의 그날 완결 사이클(n, 엔진 시간당 사이클, 승률, 평균 gross 이익/손실%)을 심볼×UTC 날짜로 잇는다. 읽는 열은 둘: `win − p_up`(추정기의 비관, NEXT 6 1항)과 `cyc/tr`(시도가 사이클로 전환되는 비율 — 곱 형태는 이것이 상수라고 가정한다, 2항). 심볼당 캠페인이 쌓여야 갈린다(NEXT 데이터 수요).
- `python -m bot.tune [--days 7 --sym A,B --keys a,b --workers 6 --apply]` — **후보마다 바구니(`books`) 전체의 테이프를 live 사이즈로 돌려 합산**한다(2026-09-02; 규칙은 공통이므로 바구니에서 이겨야 한다 — 그 전엔 TRUMP 한 심볼·고정 유닛이었다). 과적합 방지 규칙이 코드에 있다: (1) walk-forward — 마지막 날은 검증 전용, (2) 격자는 거칠고 키는 하루 2개씩 순환, (3) 후보는 현재값 ±1칸뿐(하루 한 칸 이상 못 움직임), (4) 채택 조건 전부 필요: 학습 사이클 ≥ 30, 학습 목적함수(손익 − 최대낙폭 합) ≥ 현재 × 1.10, 검증 손익 ≥ 현재, 한 칸 더 간 이웃도 현재 이상(고원이지 뾰족한 점이 아님). 결과는 `logs/tune-YYYYMMDD.json`; `--apply` 없이는 params.json을 건드리지 않는다. 초 캐시·계약 step·REST 시딩은 심볼마다 워커를 띄우기 전에 한 번 만든다(워커들이 동시에 받다 레이트리밋에 걸리면 일부 후보만 무시딩이 되어 비교가 깨진다). 새 파라미터를 튜닝 대상에 넣으려면 `SPACE`에 격자를 추가한다. 키는 요일 순환 2개에 **`PRIORITY`(live 실험 중인 키: `retrace_frac`·`rg_leg_pct`·`c1_dev`, 2026-09-02)를 매일 더한다**(`--keys`는 둘 다 덮는다). 격자는 메커니즘을 끄는 값을 담지 않는다 — 사용자가 뺀 백스톱(`trim_rest_pct`)은 격자에서 제거했고, `gate_relax`·`trim_retrace_atr`·`retrace_frac` 격자에 0이 없다. 격자 키: dip_min_atr, v_fast, v_slow, hold_s, c1_on, pop_min_pct, unit_min_pct, step_add_atr, gap_rebuy_pct, derisk_pct, gate_relax, trim_retrace_atr, retrace_frac, trim_taker_after_s, trim_taker_slip_pct, rg_drift, rg_counter_max, rg_window, rg_drift_min_pct, against_regime_mult, stop_buffer_atr, brk_atr, derisk_core_frac.

- `python -m bot.trade [--sym SYMBOL] status|open|add|tpsl|close|limit|tp|cancel` — 조회와 사용자가 시킨 수동 조작. 심볼 기본값은 `params.json` `strat.symbol`(엔진 계약). 진입은 프리셋 SL을 싣고 나가고 tpsl은 modify라 무보호 구간이 없다. 엔진 소유 (심볼, 방향)의 주문·플랜은 사용자가 시켜도 엔진을 통해서만(STOP/PAUSE/params) 다룬다.
- `python -m bot.supervise nightly` — 매일 00:10 UTC에 `logs/nightly-YYYYMMDD.txt`: **`books`의 심볼마다** 전날 신호의 15분 후 성적을 신호원(`v`/`1m`)·거래량 소멸·CVD·가치영역·레짐별로 분할(`bot.replay --sym --quiet --day --by ...`, 전날 마지막 2시간을 워밍업으로)·legs·sides/follow(live 사이즈)·sweeps, 바구니 단위로 capture·recon·체결 비용(`bot.slip`)·추정기 대 실적(`bot.pair`)·튜너 리포트(적용 없음)·스캐너. Monitor가 `REPORT` 줄을 올린다. 게이트·사이즈 승격은 이 표가 며칠 연속 갈릴 때만. 분할 키에 `U`/`D`(정체 크기)와 `side_hint_1h`/`daily_trend`가 있다 — 큰 정체 뒤 지속(반익반본 가설)은 이 표로만 검증하고 규칙화하지 않는다. `dbl`(한 봉 양방향 발화)·`brk`(브레이크 중 감속)도 같은 자리에서 본다. **방향 메커니즘의 증거**: 그날 테이프를 롱/숏/쌍검으로 백테스트한 세 줄과 `by_hint`(book별 실현손익을 그 순간의 1H 구조 힌트로 분할). 추세 쪽 키우기(역행 쪽 축소)는 이 표가 며칠 갈릴 때 — 지금은 쌍검이고 기울임은 없다(NEXT 2). **표는 `books`의 심볼마다 따로 나온다**(2026-09-02 — 그 전엔 `--sym` 없이 TRUMPUSDT 기본값으로 돌아 바구니 뒤엔 녹화도 안 되는 심볼을 재고 있었다); 튜너는 바구니 전체를 한 후보로 판정한다.
- `python -m bot.supervise sweep` — 수수료 페이백은 매일 ~07:00 UTC에 spot USDT로 입금된다(`REBATE_REWARDS`). 10분마다 spot USDT 가용액을 읽어 1 USDT 이상이면 전액(소수 4자리 내림)을 `usdt_futures`로 이체(`/api/v2/spot/wallet/transfer`). 이벤트 `SWEEP`(amount, transfer_id); 실패는 `SWEEP_FAIL`(alert)이고 다음 확인 때 재시도. 복리는 엔진의 자본 비례 사이즈(`unit_frac` 등, 포지션 0일 때 재계산)로 흡수된다. USDT 외 코인은 건드리지 않는다.

## 감독 (어느 세션이든 이어받는 절차)
1. 메모리 `cycle-harness-plan` + 이 문서를 읽는다. `logs/state.json`으로 현재 포지션·주문·스탑·레짐·오류를 확인한다.
2. Monitor에 `python -u -m bot.watch_cycle 3600`을 persistent로 붙인다(알림·체결·재기동·60분 HB만 흘러옴). /loop는 필요 없다.
3. 알림별 대응: `HALT`(DAILY_LOSS·DAILY_STOPS·EXTERNAL_FILL·STOP_FAILED·RECONCILE) → 원인 확인 후 `RESUME` 파일 생성 또는 사용자에게 보고. `STOP_HIT` → 쿨다운 뒤 자동 재진입이므로 기록만. `WS_DOWN` 60초 이상 → 감시견이 못 살리면 프로세스 상태 확인. `MARGIN_LOCKED` → 사용자 수동 포지션이 증거금을 잠근 것, 보고. `REGIME_CHANGE` → 라벨만 확인; 종목/방향 변경은 포지션 0일 때 `params.json`으로.
4. 규칙이 바뀌면 이 문서 → `bot/signal.py`·`bot/cycle.py` → `python -m unittest bot.test_signal bot.test_cycle`(불변식: 회계·유닛당 신호·cap 예산·스탑 불리 이동 금지·derisk 래치·레짐·체결 모델 공유 / OMS: 손절 체결 식별·분할 집계·취소 확인 후 테이커·스탑 중복 방지·파라미터 검증) → `python -m bot.backtest data/ws/pub-20260829-0[4-6].jsonl.gz`(기준 테이프: 캐스케이드 날; 현재 수치는 도구 절, 저녁 테이프 07–10도 같이) → 자식 프로세스 재기동(`-m bot.cycle` 프로세스를 Stop-Process; 포지션·스탑·쿨다운·사이즈는 state.json에 남는다) → `python -m bot.preflight` 순서.
5. 프로세스 확인: `logs/cycle.log`의 마지막 `SUPERVISOR start pid=`, `logs/record.log`의 15분 `REC` 줄. 둘 다 없으면 `python -m bot.supervise record|cycle`을 분리 프로세스로 다시 띄운다.
