# NEXT — 미뤄둔 메커니즘 작업 (수정 세션 백로그)

독자는 다음 수정 세션. 항목마다 "무엇이 열려 있고, 무엇이 판정하고, 언제 착수하는가"만 적는다. 결정된 것은 RULES/CONCEPT에 있고 여기 없다. 원칙: 메커니즘은 CONCEPT의 문장에서 나오고 숫자는 튜너가 옮긴다; 착수 조건은 데이터 수요이지 달력이 아니다.

## 데이터 수요 (live 08-30 기준: 하루 신호 ~300, arm ~45, 체결 ~100, 레그 ~30, 사이클 ~40)
| 필요한 것 | 하루 | 게이트 | 일수 |
|---|---|---|---|
| 레그 표(검출기 지연·거리, 레짐/사이드별 분할) | ~30 레그 | ≥ 100 레그 + 급락 아닌 날 1일 이상 | 3~4 |
| 피처 분할(신호 × 피처 버킷 15분 성적) | ~300 신호 | 버킷당 n ≥ 100 | 2~3 (dbl·brk 같은 희소 버킷은 5+) |
| 스탑헌팅 표(sweeps) + B의 live 실행(prem_broken derisk) | 60 sweep, derisk 수 건 | derisk 실행 ≥ 10건, sweep 표 5일 | 5 |
| 튜너 walk-forward | ~40 사이클 | 훈련 ≥ 2일, 검증 1일 | 3~5 |
| 방향 꼬리(급락·급등) | 드묾 | 기다리지 않는다 — 벤치로 판정 | — |

권고: **1·3·4는 5일 뒤(2026-09-04~05)에 한 세션**, 2는 데이터가 아니라 결정이라 그 전에라도 가능(단 아래 쌍검+스케일 변형 테이프는 먼저).

## 1. 속도 모델 — 담기·덜기 타이밍
- **열린 질문**: 감속 검출기가 바닥 전에 너무 일찍 발화한다(legs 표: premature 83%, 첫 발화가 바닥 −0.7%·−523초; `v`는 슬라이드에 눈멀고 `1m`은 첫 멈춤). 세 번째 `src`(ATR 상대 감속, 후보 s8: actionable 57% vs v 21%/1m 7%)를 넣을지, 넣으면 어떤 임계로.
- **ATR 부풂**: ATR(1분 Wilder, 100봉 ≈ 100분)과 ATR15(15분 Wilder ≈ 3.5시간)가 급락 뒤 몇 시간 부풀어 있다. 그 동안 `dip_min_atr`(눌림 최소 크기 3 ATR)·`refire_atr`·`brk_atr`·`trim_retrace_atr`(되돌림 꼭지 확인)·`rg_drift`(ATR 단위 drift)·`step_add_atr`(사다리 간격)·`stop_buffer_atr`·피벗 임계(k×ATR15)가 전부 커져, 급락 직후의 작은 반등이 늦게 인정되고 눌림이 신호가 못 된다. σ(1초 수익률, vol_hl 300초)도 같이 부풀어 `v`가 작아진다. 후보: 중앙값 기반/짧은 반감기 ATR, 또는 부풀기 전 값의 유지(캔들 몇 개의 최소값).
- **레짐을 현재 레그로**: 후행 90분 창 대신 마지막 확정 피벗부터의 drift(같은 zigzag 기계, θ 0.7%). 제때 켜지면 derisk 근거 복귀(회복 의미론: 첫 반대 스윙 ≥ θ에서 해제), 담기 크기, 플랫에서의 쪽. 위험: 빠를수록 극단(캐스케이드 바닥)에서 켜진다 — 급락 뒤 첫 감속 담기를 절대 막지 않는 제약 아래 설계. `rg_drift_min_pct` 2%가 −1% 그라인드 뒤 급락(08-30 19:13 직전 DEAD)을 못 읽는 것도 여기서.
- **벤치**: `bot.legs`(검출기별 지연·거리·actionable), `bot.replay`(hold vs sell), `bot.backtest`(기준 수치 RULES 도구 절), `--follow regime`.
- **착수 조건**: 레그 ≥ 100, 급락 아닌 날 포함.

## 2. 방향 — 구조 추종 플립 / 쌍검+사이즈 스케일 / 롱 온리 / 콜
- **현재 상태**: 방향은 params(select가 씀)로 고정. 반사실 벤치 `bot.backtest --follow 15m|1h|brk|regime`(플랫에서만 전환). 27h 테이프: 롱 −4.9 / 숏 +5.0 / 쌍검 −7.0(B); 전환 규칙은 전부 롱 고정과 동급이거나 나쁨 — 1H 구조는 급락·흘러내림 내내 "롱", 15m은 급락 18시간 뒤에야 숏, brk는 7번 뒤집혀 손해, 레짐 라벨은 저점 26분 뒤. 롱·숏 차이는 거의 전부 급락 2시간이고 그 뒤 24시간은 비슷하다 → **방향의 가치는 꼬리에 있고, 꼬리 전에 서 있는 쪽은 규칙이 아니라 콜.**
- **아직 안 잰 것**: 쌍검 + 역방향 반사이즈(`against_daily_mult`/`against_regime_mult`로 역방향 책만 줄이는 변형) — 백테스트 변형 필요. 일봉 추세 판독은 08-19 펌프의 EMA20 잔상으로 'up'이라 롱에 반사이즈가 한 번도 안 걸렸다(정의 재검토: 펌프 뒤 분배 국면을 'down'으로 읽어야 한다).
- **세 층 구조(합의)**: select = 코인과 며칠 단위 국면 방향, 레짐 판독 = 하루 안 쪽·크기(플랫에서), 순환매 = 분. 1과 같은 세션에서 — 현재 레그 판독이 세 소비자(쪽·크기·derisk)에 다 들어간다.
- **착수 조건**: 결정. 쌍검+스케일 변형 테이프를 먼저 뽑고, select의 "전환 시 1H 구조로 방향" 규칙은 위 반증으로 재검토.

## 3. 스탑헌팅
- **방어(끝)**: B — 거래소 스탑 = 돈 한도, 구조가 = derisk 래치(RULES 스탑 절). live 관찰 항목: `prem_broken` derisk의 실행 수·체결가 대비 결과, 전제 아래에서 반등 없이 cap/청산가 가드까지 간 사례, 1~2유닛에서 가드(≈ −8%)가 실제 스탑이라는 사실이 문제가 되는지(cap_frac 0.15 = 지갑 15%).
- **공격(미착수)**: sweep & reclaim(확정 피벗 아래로 쓸고 되찾음)을 세 번째 검출 `src`로 — 레벨이 "어디", 되찾음이 "언제". 첫 표(하락일): 되찾은 뒤 롱 진입은 15분 −0.11%로 근거 없음, 고점 sweep 되찾은 뒤 숏은 +0.71%. 며칠 표가 갈릴 때 1과 함께.
- **버퍼**: `stop_buffer_atr`는 B에서 "전제가 깨졌다고 보는 거리"다(튜너 격자). sweeps 표(되찾은 sweep 중 버퍼보다 깊은 비율)가 근거.
- **벤치**: `bot.sweeps`(야간), 야간 리포트, events의 derisk 체결.

## 4. 기록만 되고 Strategy가 읽지 않는 피처
- 볼륨 프로파일 `vp_dens/vp_va/vp_sup/vp_res/vp_poc`, 30초 체결 흐름 `buy30/sell30/dmid30`, 60초 거래량 `vol60`, CVD 다이버전스 `cvd_div/cvd_div_bear`, 호가 재충전·불균형·매수비중 `bid_refill/ask_refill/imb/bs10`, 감속비 `dec`, 거래량 소멸 `sell_spike/sell_decay/sell_now`(매수 쪽 동일), `dbl`(한 초 양방향 발화), `brk` 분할, `side_hint_15m`.
- **원칙**: 게이트(거부권)가 아니라 **크기·확인**으로 들어간다(레짐 거부권을 뺀 이유와 같다). 하나의 피처가 신호의 15분 성적을 버킷 간 유의하게 가를 때만.
- **벤치**: 야간 리포트 signals 절(`bot.replay --by ...`, hold vs sell 포함). `add_confirm`(두 규칙 동시·거래량 소멸·되돌림)은 이미 쌍검 자동 on의 확인 규칙이다 — 그 성적도 같은 표에서.
- **착수 조건**: 버킷당 n ≥ 100, 같은 방향의 차이가 2일 이상 반복.

## 5. FAVOR 코어 홀드 + 트레일 (사용자가 빠뜨린 것)
- 상승분 아래만 먹는 문제("깔짝"): `bot.capture` — 상승 레그 보유 27% vs 하락 35%. 사이클 척도의 코어 홀드(FAVOR에서 코어를 정체에 안 팔고 트레일)는 "수익 극대화 로직을 순환매에 얹지 않는다"는 사용자 원칙과 충돌하므로 측정 뒤 결정. 벤치: capture, replay hold05/hold15(첫날: 정체에서 파는 게 맞았음 −0.04%).

## 6. 종목 선정 검증 (사용자가 빠뜨린 것)
- 캔들 프록시(`scan.proxy`) 순위가 틱 백테스트 순위와 맞는지 — 후보 종목 녹화(record top-5 + watchlist)로 검증. PROMUSDT 덤프 국면 숏 백테스트(테이프 생기면). "하루 급락은 순환매 천국" 플래그 의미가 select의 concept 지표에 반영됐는지. 벤치: `bot.scan --json`, `bot.select` 로그, 후보 심볼 백테스트.

## 7. 운영 판단
- 튜너 `--apply` 승인 시점: 훈련 ≥ 2일 + 검증 1일이 처음 갖춰지는 날(09-01~02)부터 리포트를 보고 키 단위로 승인. 격자에 "off" 값은 없다(메커니즘 스위치는 튜너가 만지지 않는다).
- 백테스트 현실성: 청산가 가드·펀딩 없음(cap/명목 비율은 cap_frac 0.15로 live와 같아짐).

## 결정된 것 — 다시 제안하지 않는다 (근거는 RULES)
테이커 담기 전환(저녁 +0.54 → −1.7) · ATR 스케일 레짐 스윙 임계(캐스케이드 바닥에서 켜짐, −16.3 → −20.3) · 3m EMA 진입 게이트(과적합) · 담기 TTL 90초 초과(300~1800초 전부 악화) · 레짐 거부권(반사이즈로 대체) · 구조 하드 스탑(B로 대체) · 담기 큐 대기의 시간축(큐가 원인).
