# NEXT — 미뤄둔 메커니즘 작업 (수정 세션 백로그)

독자는 다음 수정 세션. 항목마다 "무엇이 열려 있고, 무엇이 판정하고, 언제 착수하는가"만 적는다. 결정된 것은 RULES/CONCEPT에 있고 여기 없다. 원칙: 메커니즘은 CONCEPT의 문장에서 나오고 숫자는 튜너가 옮긴다; 착수 조건은 데이터 수요이지 달력이 아니다.

## 데이터 수요 (쌍검 전환 뒤 08-31 기준: 하루 신호 ~300, arm ~45, 체결 ~100, 레그 ~30, 로트 사이클 ~99, 캠페인 ~70)


| 필요한 것                                           | 하루                   | 게이트                          | 일수                         |
| ----------------------------------------------- | -------------------- | ---------------------------- | -------------------------- |
| 레그 표(검출기 지연·거리, 레짐/사이드별 분할)                     | ~30 레그               | ≥ 100 레그 + 급락 아닌 날 1일 이상     | 3~4                        |
| 피처 분할(진입 피처 × 캠페인 성적)                           | ~70 캠페인              | 3분할 버킷당 n ≥ 100 = 캠페인 ≥ 300  | 4~5 (dbl·brk 같은 희소 버킷은 그 뒤) |
| 스탑헌팅 표(sweeps) + B의 live 실행(prem_broken derisk) | 60 sweep, derisk 수 건 | derisk 실행 ≥ 10건, sweep 표 5일  | 5                          |
| 튜너 walk-forward                                 | ~70 캠페인              | 훈련 ≥ 2일, 검증 1일               | 3~5                        |
| 방향 꼬리(급락·급등)                                    | 드묾                   | 기다리지 않는다 — 벤치로 판정            | —                          |


권고: **8(포트폴리오)이 먼저다** — 사용자 결정 2026-09-01, 지금 우선순위가 수익이 아니라 스크립트 고도화이고, 종목 질문은 백테스트로 못 푸는 것이 측정으로 확인됐기 때문이다(측정 규약). **5는 데이터 게이트가 아니라 벤치 게이트(backtest 반사실)라 언제든 할 수 있고 표에서 값이 제일 크다.** 1·3·4는 캠페인 300개가 차는 09-03~04에 한 세션. 2는 데이터가 아니라 결정이라 그 전에라도 가능. 다만 **5와 1·3·4의 backtest 근거는 recon 오차(창당 sd 1.34)를 진다** — 큰 스윙만 신뢰한다.

## 측정 규약 — 아래 모든 항목에 적용

이 절이 없으면 다음 구현자는 성적표에서 필터를 골라 params에 넣고, 그건 전부 과적합이다.

- **질의 단위는 포착폭 / 통행료다.** live 152 로트 사이클: 왕복 수수료가 명목의 **0.0484%**, 사이클당 평균 gross **0.0892%** — **수수료가 gross의 54.2%**. 통행료는 고정이고 포착폭만 테이프에 비례하므로, 횟수를 무작위로 줄이면 gross와 fee가 같이 줄어 순익은 정확히 비례 축소된다. **필터는 조건부 기댓값을 바꿀 때만 값이 있고, 그 외에는 규모 축소일 뿐이다.**
- **로트 표와 캠페인 표는 서로 반대로 편향돼 있다. 어느 쪽으로도 담기 규칙을 판정할 수 없다.**
  - 로트 단위(`bot.cycles`): 추가 로트는 규칙상 자기 매수가 위에서만 팔리므로 거의 항상 이익이고(depth 2·3, n=49 **+15.97**), 코어는 평단 기준으로 팔리므로 평단이 내려간 몫을 떠안는다(depth 1, n=103 **−7.51**). 이 분할은 질의 측정이 아니라 게이트 규칙의 회계 항등식이다.
  - 캠페인 단위(플랫→플랫): 다중 로트 캠페인은 **가격이 역행했기 때문에** 다중이 된 것이라 결과에 조건화돼 있다(단일 로트 74개 **+18.54** / 2로트 이상 31개 **−9.60**). "담기가 손해"로 읽으면 안 된다.
  - 따라서 **반사실은 `bot.backtest` 재실행뿐이다.** 담기 하나를 거르면 사다리 앵커·`gap_rebuy` 기준가·다음 신호의 depth·가용 증거금이 전부 바뀐다. 표에서 사이클을 지우고 합을 다시 내는 시뮬(그렇게 하면 +8.46 → +14.20까지 나온다)은 알려진 패자를 골라낸 것이며 값이 아니다.
- **한 축의 여러 프록시를 여러 게이트로 만들지 않는다.** `dip%`·`|v|`·`vol60`·`dec`·`atr%`·`brk/bko`·`vp_va`는 서로 다른 피처가 아니라 "지금 테이프가 실제로 움직이는가" 한 축의 프록시다. 단위를 로트에서 캠페인으로 바꾸면 이 중 대부분이 무너진다 — `vol60`은 평평해지고(+2.90/+2.49/+3.55), `imb`는 부호가 남지 않고, `atr%`는 정상일에서 U자가 된다. `|v|`만 3일 모두 방향이 유지됐다(그래도 후보일 뿐이다).
- **백테스트 자체의 오차를 먼저 읽는다 (2026-09-01)**: `bot.recon`이 경계 이벤트(재기동·HALT·파라미터 변경·전환·인수)가 하나도 없는 연속 시간대만 골라, 그 구간의 실제 `SIZING` 값으로 돌린 백테스트를 실매매 장부와 대조한다. 첫 8구간(08-30·08-31, 26시간)의 구간 오차: **+1.95 / +0.25 / −0.13 / −0.44 / +0.04 / +0.08 / −2.79 / −1.16 → 평균 −0.28, sd 1.34, t = −0.58.** 부호가 4:4라 **계통 편향이 아니라 무작위 오차**이고, 그게 더 나쁜 경우다 — 계통 오차는 종목 간 비교에서 상쇄되지만 무작위는 잡음을 더한다. 시간당 sd ≈ **0.47/시간**인데 이 도구로 잰 TRUMP−ZEC 차이는 **+0.138/시간**이었다: **도구의 잡음이 재려던 효과의 3배다.**
  - 구조적 원인은 8구간 전부에서 같다. 담기 주문 수는 live와 맞는데(11/10, 15/19, 11/11, 5/5 …) **덜기가 절반 수준**(16/9, 19/15, 14/7, 12/8 …)이다. 메이커 체결 모델이 실제보다 엄격해(배치 시점 큐 소진 또는 관통 프린트를 요구) 청산이 덜 나가고 포지션을 더 오래 든다 — 그래서 하락 구간에선 백테스트가 더 나쁘고 상승 구간에선 더 좋다. 부분 체결 때문이 아니다(고유 clientOid로 셌고 주문당 조각은 1.05~1.27).
  - **판정에 미치는 영향**: 같은 종목·같은 테이프에서 본 **큰** 차이는 이 오차가 못 건드린다(사다리 `max_units=1` 10.16 스윙, 퍼센트 재스케일 5~12 악화, `max_units` 7 무변화). **창당 1.5 미만의 차이에 기댄 결론은 무효로 취급한다 — TRUMP vs ZEC이 여기 해당한다.** 그리고 체결 모델의 현실성이 틱 크기에 의존하므로(TRUMP 0.042% vs ZEC 0.001%, 40배) 종목 간에는 오차가 **비대칭**일 수 있다 — 종목 비교에서 제일 위험한 형태다.
  - 야간 리포트가 매일 구간을 쌓는다. 무작위/계통 판별과 체결 모델 수정 여부는 구간이 더 모인 뒤에.
- **판정은 셋을 다 통과해야 한다**: 하루별 재현 + 메커니즘 설명 + 기준 테이프 out-of-sample. 셋 중 하나라도 없으면 항목에 후보로 남기고 params에 넣지 않는다. 튜너 격자도 다중검정이라 같은 조건을 진다.

## 1. 속도 모델 — 담기·덜기 자리

- **언제 (열린 질문)**: 감속 검출기가 바닥 전에 너무 일찍 발화한다(legs 표: premature 83%, 첫 발화가 바닥 −0.7%·−523초; `v`는 슬라이드에 눈멀고 `1m`은 첫 멈춤). 세 번째 `src`(ATR 상대 감속, 후보 s8: actionable 57% vs v 21%/1m 7%)를 넣을지, 넣으면 어떤 임계로.
- **얼마나 큰 자리에서 (열린 질문)**: 두 검출기가 같은 질문에 **다른 단위와 다른 창**을 쓴다 — `v`는 600초(`swing_s`) 스윙 대비 `dip_min_atr` 3 ATR, `1m`은 30봉 고점 대비 `c1_dev` 0.4%. 신호의 90%가 1m에서 나오므로(767개 중 **1m 689 / v 78**, 체결까지 간 152 사이클도 127:25) 실질 크기 조건은 0.4%이고 `dip_min_atr`는 소수 검출기만 문지른다. 집안 관례는 이미 "ATR 임계에 % 바닥"이다(`step = max(step_add_pct, step_add_atr×ATR15)`, `rg_drift` + `rg_drift_min_pct`) — `dip_min_atr`는 남은 순수 ATR 임계 중 하나다. 두 검출기의 크기 조건을 같은 단위로 맞출지, ATR 쪽에 % 바닥을 씌울지, 그대로 둘지.
  - **반증(2026-09-01)**: 결과 표로 이 질문에 답하려던 시도는 실패했다. 눌림 크기 %를 캠페인 성적에 걸면 급락일(08-30)에서만 갈리고(<0.55% −10.11 vs ≥0.55% +3.17), 캠페인 70개가 있는 정상일(08-31)에서는 **역전된다**(하위 +4.40 / 중위 +4.09 / 상위 +2.02). 지금 데이터는 크기 조건을 조이는 근거가 되지 못한다 — 단위 정리는 개념 문제로 남기고, 임계를 움직이려면 급락 아닌 날 캠페인이 더 필요하다.
- **`src`는 이진 라벨로 쓰지 않는다**: `v`가 낸 사이클은 승률 84%지만 순손익 ~0(n=25)이다. "v도 같이 발화했나"(이진)는 질의 축이 아니었다. 반면 발화 순간 `v` 모델이 그 레그에서 본 극값(`minv`/`maxv`, 연속값)은 로트·캠페인 양쪽에서 하위 버킷만 음수로 갈렸다 — 이진 `add_confirm`이 테이프에서 반증된 것이 연속 점수의 반증은 아니다. 다만 `minv`는 1m 발화 때 리셋되지 않아 한 레그의 여러 신호가 같은 값을 물려받는다(관측이 레그 단위로 뭉쳐 유효 n이 신호 수보다 작다). → 4번에서 크기·확인으로.
- **아웃티지 뒤의 `v`는 표본 하나로 만들어지는데 신뢰된다**: `Features._clock`의 아웃티지 분기(피드가 120초 넘게 끊겼다 재개)는 `self.vraw`만 리셋하고 `self.var`(σ)는 그대로 둔다. 재개 직후 `vraw.n = 1`이라 `v`는 첫 1초 수익률 하나로 만들어지는데, 바로 그걸 막으라고 있는 `ve = v if var.n >= vol_hl else 0`는 `var.n`이 큰 채 남아 그냥 통과한다 — 재시작 직후를 막으려던 가드가(2026-08-29 live `v` 신호 11개 중 8개가 START 3분 안) 아웃티지에는 걸리지 않는다. **열린 질문**: 아웃티지를 재시작과 같게 볼지(σ도 리셋 → 재개 뒤 `vol_hl`초 동안 `v` 침묵), 아니면 σ만은 아직 유효하다고 볼지. 벤치: `bot.legs`로 아웃티지 직후 발화와 START 직후 발화의 지연·거리를 비교. **착수 조건 없음** — 데이터가 아니라 의미론 결정이다.
- **ATR 부풂**: ATR(1분 Wilder, 100봉 ≈ 100분)과 ATR15(15분 Wilder ≈ 3.5시간)가 급락 뒤 몇 시간 부풀어 있다. 그 동안 `dip_min_atr`·`refire_atr`·`brk_atr`·`trim_retrace_atr`·`rg_drift`·`step_add_atr`·`stop_buffer_atr`·피벗 임계(k×ATR15)가 전부 커져, 급락 직후의 작은 반등이 늦게 인정되고 눌림이 신호가 못 된다. σ(1초 수익률, vol_hl 300초)도 같이 부풀어 `v`가 작아진다. 후보: 중앙값 기반/짧은 반감기 ATR, 또는 부풀기 전 값의 유지(캔들 몇 개의 최소값).
- **레짐을 현재 레그로**: 후행 90분 창 대신 마지막 확정 피벗부터의 drift(같은 zigzag 기계, θ 0.7%). 제때 켜지면 derisk 근거 복귀(회복 의미론: 첫 반대 스윙 ≥ θ에서 해제), 담기 크기, 플랫에서의 쪽. 위험: 빠를수록 극단(캐스케이드 바닥)에서 켜진다 — 급락 뒤 첫 감속 담기를 절대 막지 않는 제약 아래 설계. `rg_drift_min_pct` 2%가 −1% 그라인드 뒤 급락(08-30 19:13 직전 DEAD)을 못 읽는 것도 여기서.
- **벤치**: `bot.legs`(검출기별 지연·거리·actionable), `bot.replay`(hold vs sell), `bot.backtest`(기준 수치 RULES 도구 절), `--follow regime`.
- **착수 조건**: 레그 ≥ 100, 급락 아닌 날 포함. 크기 조건 쪽은 캠페인 ≥ 300.

## 2. 방향 — 구조 추종 플립 / 쌍검+사이즈 스케일 / 롱 온리 / 콜

- **현재 상태**: 방향은 params(select가 씀)로 고정. 반사실 벤치 `bot.backtest --follow 15m|1h|brk|regime`(플랫에서만 전환). 27h 테이프: 롱 −4.9 / 숏 +5.0 / 쌍검 −7.0(B); 전환 규칙은 전부 롱 고정과 동급이거나 나쁨 — 1H 구조는 급락·흘러내림 내내 "롱", 15m은 급락 18시간 뒤에야 숏, brk는 7번 뒤집혀 손해, 레짐 라벨은 저점 26분 뒤. 롱·숏 차이는 거의 전부 급락 2시간이고 그 뒤 24시간은 비슷하다 → **방향의 가치는 꼬리에 있고, 꼬리 전에 서 있는 쪽은 규칙이 아니라 콜.**
- **쌍검 + 역방향 반사이즈** (2026-08-30 23:10, B 코드; 책당 예산 반 = live 분할, `add_confirm` off, 일봉 스케일 없음 — 이 세 조건을 안 맞추면 표가 왜곡된다: 쌍검이면 `add_confirm`이 자동 on이라 사이클이 줄고, 일봉 판독 'up'이 `against_daily_mult`로 숏 책을 항상 반으로 만든다):
  
  | 27h / 급락 / 저녁                    | 합계                        | 롱 책 / 숏 책 (27h) | DD(27h)  |
  | -------------------------------- | ------------------------- | --------------- | -------- |
  | 롱 고정(풀)                          | −4.90 / −9.14 / +3.07     |                 | 25.5     |
  | 숏 고정(풀, 일봉 스케일 끔)                | +10.03 / +2.54 / —        |                 | 7.5      |
  | **쌍검, 스케일 없음**                   | **+2.70 / −3.30 / +2.42** | −2.45 / +5.30   | **10.8** |
  | 쌍검, 마지막 15m 판독 역방향 ×0.5 (k=2)    | +2.08 / −3.30 / +2.42     | −2.53 / +4.77   | 10.8     |
  | 쌍검, 같은 것 k=1                     | +0.75 / −3.93 / +1.98     | −2.60 / +3.51   | 11.8     |
  | 쌍검, 책당 풀 예산(자본 2배), 역방향 ×0.5 k=2 | +4.03 / −6.60 / +4.84     | −5.09 / +9.43   | 21.6     |
  | 15m 추종(한쪽만, 플랫 전환) k=2 / k=1     | −5.16 / −2.14 (27h)       |                 | 25.5     |
  

  읽기: **쌍검 = 두 방향의 평균**(롱 반 + 숏 반)이고 판독이 필요 없다. 세 테이프 모두 양수 또는 롱 고정의 1/3 손실(급락 −3.3 vs −9.1), DD는 절반 이하. 15m 구조로 역방향을 줄이는 기울임은 이 테이프에서 손해(판독이 늦고 — 급락 전 'long' 읽기가 급락 내내 숏 책을 반으로 — k=1은 휘둘림). 한쪽만 추종하는 플립은 쌍검보다 나쁘다(늦은 전환·휘둘림). 자본 2배 풀 예산은 자본당 같은 값. 인간 트레이더 자료("양방매매에 대하여", 나뇨띠)도 같은 형태다: 양쪽을 살려두고 추세 쪽을 키워 반대쪽 평단을 고친다; 원웨이에선 안 된다고 명시 — 우리 표에서 급락일 −3.3이 그 값이다.
- **일봉 판독**: EMA20 규칙은 08-19 펌프 뒤 몇 주 동안 'up'이다(EMA20 2.07 vs 가격 2.5~2.9, 20일 고점 대비 −28%). 일봉 구조(structure_side)는 None(펌프 뒤 큰 레인지). 즉 며칠 단위 국면은 "펌프 뒤 넓은 레인지"이고 방향은 판독이 아니라 콜이다 — 정의를 고쳐도 'down'이 나오지 않는다.
- **세 층 구조(합의)**: select = 코인과 며칠 단위 국면 방향, 레짐 판독 = 하루 안 쪽·크기(플랫에서), 순환매 = 분. 1과 같은 세션에서 — 현재 레그 판독이 세 소비자(쪽·크기·derisk)에 다 들어간다.
- **결정(2026-08-30 22:36, 사용자 승인)**: **쌍검으로 간다** — `sides: ["long","short"]`, `add_confirm: 0`(쌍검 자동 on 규칙은 테이프에서 반증: 일봉 스케일을 끈 채 confirm on −1.34/−6.84/+1.71 vs off +2.70/−3.30/+2.42), `against_daily_mult: 1.0`(일봉 EMA20 잔상 무력화), select는 종목 전환 시 쌍검 유지.
- **남는 것(다시 볼 때)**: 추세 쪽 키우기(역방향 축소) — 제때 읽는 판독(1번의 현재 레그)이 생기면 `--follow`/스케일 벤치로 재판정. 15m 구조 판독은 select의 전환 방향(`side` 기록)에만 쓰고 크기엔 안 쓴다. 일봉 판독 정의는 1번과 함께.

## 3. 스탑헌팅

- **방어(끝)**: B — 거래소 스탑 = 돈 한도, 구조가 = derisk 래치(RULES 스탑 절). live 관찰 항목: `prem_broken` derisk의 실행 수·체결가 대비 결과, 전제 아래에서 반등 없이 cap/청산가 가드까지 간 사례, 1~2유닛에서 가드(≈ −8%)가 실제 스탑이라는 사실이 문제가 되는지(cap_frac 0.15 = 지갑 15%).
- **공격(미착수)**: sweep & reclaim(확정 피벗 아래로 쓸고 되찾음)을 세 번째 검출 `src`로 — 레벨이 "어디", 되찾음이 "언제". 첫 표(하락일): 되찾은 뒤 롱 진입은 15분 −0.11%로 근거 없음, 고점 sweep 되찾은 뒤 숏은 +0.71%. 며칠 표가 갈릴 때 1과 함께.
- **버퍼**: `stop_buffer_atr`는 B에서 "전제가 깨졌다고 보는 거리"다(튜너 격자). sweeps 표(되찾은 sweep 중 버퍼보다 깊은 비율)가 근거.
- **벤치**: `bot.sweeps`(야간), 야간 리포트, events의 derisk 체결.

## 4. 기록만 되고 Strategy가 읽지 않는 피처

- **원칙(변함없음)**: 게이트(거부권)가 아니라 **크기·확인**으로 들어간다(레짐 거부권을 뺀 이유와 같다). 그리고 측정 규약대로 **하나의 축에는 하나만** 넣는다.
- **한 축의 프록시들** — `dip%`·`vol60`·`dec`·`atr%`·`brk/bko`·`vp_dens/vp_va/vp_sup/vp_res/vp_poc`·`buy30/sell30/dmid30`·`bs10`. 로트 표에서는 전부 같은 방향으로 갈렸지만 캠페인 단위로 바꾸면 대부분 사라진다. **여섯 개를 각각 넣으면 한 축에 여섯 번 과적합한 것이다.** 이 축을 쓸 거면 대표 하나를 고르고 나머지는 후보에서 뺀다.
- **살아남은 후보 하나**: `|v|`(발화 순간의 정규화 속도) — 캠페인 단위 3분할에서 하위만 음수(−8.29 / +8.08 / +9.14), 하루별로도 3일 모두 하위가 낮다. 그래도 정상일 한 날의 n=70이라 착수 조건 미달. 같은 축의 연속 점수인 `minv`/`maxv`(1번 참조)와 함께 본다.
- **약한 음성 결과(참고, n 부족으로 결정 아님)**: `vd_gate=1`로 거래량 소멸을 요구하면 남는 건 n=24, 승률 88%인데 순손익 −4.13(드물게 크게 잃음). `dbl`(한 초 양방향 발화) n=12, −2.93. `imb`(호가 불균형)는 로트 표에서 직관과 반대 부호로 갈렸으나 캠페인 단위에서 사라졌다 — 다중검정의 표본으로만 기억한다.
- **`brk/bko`가 켜진 채로 낸 진입이 더 나았던 것은 현재 설계의 확인이다**: `derisk_on_breakdown`은 **이미 들고 있던 포지션을 때린** break에만 반응하고 break 뒤 감속 담기는 그 break의 피해자가 아니다 — 코드가 이미 그렇게 갈라놨다(`brk_seen`). 이 구분을 없애자는 제안은 하지 않는다.
- **벤치**: 야간 리포트 signals 절(`bot.replay --by ...`, hold vs sell 포함), 그리고 후보마다 `bot.backtest` 재실행(측정 규약).
- **착수 조건**: 캠페인 ≥ 300(버킷당 100), 같은 방향의 차이가 급락 아닌 날 2일 이상 반복.

## 5. 진입 이후의 정책 — 거부 카운터, 장기 캠페인, 사다리 반사실, FAVOR 코어

지금 표에서 값이 제일 큰 항목이다. **장기 캠페인 11개(>1h)의 손실 −15.30이 시스템 전체 순익 +8.94보다 크고**, 자본×시간의 39%를 쓴다. 나머지 94개는 +24.24다.

- **거부 카운터(`fail_n`)가 가장 잘 갈리는 축이다** — 그리고 진입 피처가 아니라 **진입 이후의 상태**다. 캠페인 105개:
  
  | `fail_n` 최대 | n | 순손익 | 승률 | 중앙 보유 | 평균 로트 수 |
  | ----------- | --- | ------ | --- | ------ | ------- |
  | 0           | 34  | **+13.88** | 82% | 9.5m   | 1.2     |
  | 1~2         | 62  | −2.93  | 65% | 23.1m  | 1.5     |
  | 3~5         | 8   | −0.74  | 50% | 47.4m  | 1.8     |
  | ≥ 6         | 1   | −1.28  | 0%  | 84.9m  | 3.0     |
  
  시장이 정체를 한 번도 거부하지 않은 캠페인이 시스템 전체 순익보다 많이 벌고, 한 번이라도 거부당한 캠페인은 합쳐서 음수다. 조건화된 양이므로 **필터가 아니라 상태 관측**으로만 쓴다.
- **열린 질문**: CONCEPT은 이미 이 양을 지목한다 — *"담은 물량의 기대가 거듭 무산되면 눈높이를 낮춰 정리한다 — 기준은 시간이 아니라 시장이 반등을 거부한 횟수다."* 엔진은 `fail_n`을 세고 있지만 **오직 게이트를 낮추는 데만** 쓴다(`g_rel = floor + (g_norm−floor)×(1−gate_relax)^fail_n`). 같은 카운터가 담기 유닛을 줄이거나 derisk를 무장시키는 근거가 되어야 하는가. 시간 기준은 CONCEPT이 명시적으로 배제하므로 후보가 아니다.
- **평단 기준 출구를 정교화할 것 (2026-09-01 넣음, 계약은 RULES 덜기 절)**: 추가 유닛이 평단보다 나쁜 자리에 남으면 코어와 같은 게이트로 평단 대비 팔게 했다. 메커니즘 근거는 확실하지만(엔진이 이익 나는 덜기를 거부하고 있었다) **측정 근거가 없다** — 기준 테이프 4개 전부 무변화이고, 그 국면이 27h 중 204초(0.11%)·덜기 신호가 겹친 초는 3뿐이다. 지금 live 는 유닛이 5배가 되고 `notional_frac` 으로 사다리가 실제로 차면서 다중 로트 캠페인이 훨씬 흔해진 상태라, **관측은 앞으로 쌓인다**. 다시 볼 때 네 가지: (1) 평단 게이트가 로트 게이트와 **같은 `fail_n`을 공유**한다 — 거부는 로트의 눈높이에 대해 세어졌는데 평단 눈높이가 그 이력을 물려받는 게 맞나. (2) 바닥이 0.0(코어와 동일)이라 `fail_n`이 커지면 총손익 ~0에 수수료를 내는 덜기가 가능하다 — 코어의 기존 성질과 같지만 바닥을 수수료로 둘지는 열려 있다(코어 바닥과 함께 판정). (3) 폴백이 아니라 **항상 로트/평단 중 유리한 쪽**으로 게이트하는 게 맞는지. (4) `bot.cycles`의 로트 단위 표에서 추가 로트가 음수 gross 를 낼 수 있게 되므로 depth 별 비대칭이 완화된다 — 측정 규약의 "로트 표는 회계 항등식" 서술을 그때 다시 잰다.
- **장기 캠페인의 실제 청산 경로 — 진단을 틀리지 말 것**: >1h 캠페인 11개 중 8개는 `dev_lot`가 **양수**인 자리에서(retrace/normal 모드) 나갔고, derisk(−`derisk_pct` 게이트)로 나간 건 2개, 스탑 1개다. 즉 "코어가 손실에서 못 빠져나온다"는 진단은 **틀렸다** — 코어의 손실 탈출 경로는 있고(derisk에서 게이트가 −3%까지 내려간다), 실제로 −0.99%까지 발화한다. 이 캠페인들이 잃은 이유는 코어가 **평단 기준**으로 팔리기 때문이다(`ref = avg`): 아래에서 담아 평단이 내려간 만큼 코어 로트는 자기 매수가 아래에서 팔린다. 그건 CONCEPT이 설계한 대로다. **열린 질문은 "왜 못 나갔나"가 아니라 "그 대기가 값어치를 했나"이고, 벤치는 `bot.replay`의 hold vs sell이다.**
- **사다리 반사실이 한 번도 측정된 적 없다**: 단일 로트 캠페인 74개가 +18.54, 2로트 이상 31개가 −9.60. 조건화 때문에 "담기가 손해"로 읽으면 안 되지만(측정 규약), **담기 사다리가 손실을 떠안는 부분이라는 것과 그 반사실이 비어 있다는 것은 사실이다.** 벤치: `bot.backtest`를 `max_units` 1과 현재값으로 같은 테이프에 돌린 비교. 이건 튜닝이 아니라 CONCEPT 층위의 확인이다(사이클이 평단을 옮긴다는 주장 자체의 값).
- **FAVOR 코어 홀드 + 트레일**: 상승분 아래만 먹는 문제("깔짝") — `bot.capture`: 상승 레그 보유 27% vs 하락 35%. 사이클 척도의 코어 홀드(FAVOR에서 코어를 정체에 안 팔고 트레일)는 "수익 극대화 로직을 순환매에 얹지 않는다"는 사용자 원칙과 충돌하므로 측정 뒤 결정. 벤치: capture, replay hold05/hold15(첫날: 정체에서 파는 게 맞았음 −0.04%).
- **`core_units`를 "보호되는 물량"으로 읽지 말 것**: 같은 파라미터가 FAVOR에서는 코어를 지키고 derisk에서는 코어를 단계적으로 자르는 데(절반 → 나머지) 쓰인다. 방향이 국면에 따라 뒤집힌다.
- **착수 조건**: 없음 — 데이터가 아니라 벤치(backtest/replay 반사실)가 판정한다. 1보다 먼저 할 수 있다.

## 6. 종목 선정 검증

**프록시 검증은 끝났고 프록시는 판정에서 빠졌다**(수치·새 규칙은 RULES 도구 절). 남은 것만 적는다.

- **프록시를 고칠 것인가 버릴 것인가**: 어긋남의 원인은 셋이고 전부 같은 방향(사이클이 많을수록 손해)이다 — (1) 덜기 수수료를 전부 테이커로 매김(이것만으로 TRUMP 08-30이 +3.54 → −0.45), (2) 1분 종가 체결(진입이 국소 극점이라는 전제가 1분 입자도에서 사라진다), (3) derisk 없음. (1)은 "메이커/테이커 혼합비를 몇으로 두나"라는 답 없는 모델 판단이고 종목마다 다르다 — 지금 데이터로 정하면 TRUMP 하루에 맞추는 것이다. 판정: 여러 종목·여러 날의 틱 백테스트를 라벨로 두고 (2)를 먼저 고친 프록시가 순위를 맞히는지. 못 맞히면 프록시를 지우고 concept만 남긴다(노브 제거는 이 데이터 예산에서 공짜다 — 측정 규약).
- **concept의 검증은 아직 얕다**: 2일·8종목·순위상관 ρ ≈ 0.4다. 맞힌 것은 "현직이 1위"라는 한 가지뿐이고 그게 전환 규칙이 의존하는 부분이라 지금은 충분하지만, concept이 **일반적으로** 좋은 랭커라는 근거는 아니다. 특히 HYPE는 concept 2위인데 실측 최하위였다. 착수 조건: 종목 ≥ 5, 급락 아닌 날 ≥ 2.
- **TRUMP가 이긴 것은 종목 우위와 파라미터 적합을 구분하지 못한다**: `dip_min_atr`·`c1_dev`가 TRUMP에서 정해졌다. 해석의 비대칭을 지켜라 — **도전자가 이기면 강한 증거**(적대적 파라미터에도 이겼다), **현직이 이기면 약한 증거**. 반대로 "프록시가 틀렸다"는 강한 증거다: 프록시는 같은 파라미터의 같은 엔진을 근사한 모델이라 적합이 양쪽에서 상쇄된다.
- **종목별 파라미터 이전성**: 크기 조건 중 `c1_dev`(0.4%)만 퍼센트이고 나머지는 ATR 상대다. 종목을 바꾸면 이 하나만 절대 척도로 남는다 — 1번의 단위 질문과 같은 항목이다.
- **방법(다음 사람이 다시 헤매지 않도록)**: 같은 테이프에서 다른 종목을 돌리려면 `--sym`에 더해 **그 종목의 실제 `qstep`(`--qstep`)과 명목가를 맞춘 `unit_qty`가 둘 다** 필요하다(ZEC $859 × 유닛 70이면 명목 한도에 걸려 0 사이클). `cap_usdt`·`max_notional`·`daily_loss_limit`도 같은 배수로 올려야 캠페인 기하가 보존된다 — 하나라도 빠지면 다른 전략을 비교하는 셈이다. 스케일 중립성은 확인됨(×1/×5/×10에서 유닛당 −0.79/−0.95/−0.99, 차이는 qstep 입자도).
- **`concept`이 못 보는 것은 기회가 아니라 capacity다 — `imp%`로 이제 보이지만 아직 판정에 안 쓴다**: `scan`이 종목마다 제곱근 충격 `imp% = σ_daily × √(유닛 명목가 / 24h 거래대금)`을 계산해 표·`scan.json`·`SELECT` 이벤트에 싣는다(적합 상수 없음; 유닛 명목가는 `state.json`의 equity에서). 2026-09-01: **TRUMP 0.0259%(왕복 수수료 0.048%의 54%)** vs ZEC 0.0096%(20%) / SUI 0.0105% / HYPE 0.0081% / SOL 0.0036%(8%). 하루 전에는 TRUMP도 0.0080%(17%)였고 증가분은 전부 우리 쪽이다 — **√(유닛 4.9배) × √(거래대금 2.1배 감소) = 3.2배**. **열린 질문 셋**: (1) **보정** — 제곱근 법칙은 기관 참여율용이고 우리 유닛은 ADV의 ~20ppm이라 이 값은 상한으로 읽어야 한다. 더 직접적인 관측은 `PLACE.queue` 대비 유닛 비중이다(08-30 2.5% → 09-01 20.7%). 캘리브레이션 도구는 **틱 백테스트뿐이다** — 녹화된 호가 깊이와 배치 시점 큐로 체결시키므로 유닛 ×1/×2/×5/×10을 같은 테이프에 돌려 per-unit 성적이 √(사이즈)로 떨어지는지 재면 상수가 나온다(예비: ×1/×5/×10에서 유닛당 −0.79/−0.95/−0.99인데 단조 √는 아니고 사이클 구성도 같이 변했다). (2) **게이트로 쓸 것인가** — 자연스러운 자기참조 임계는 "`imp%` ≥ 왕복 수수료"(우리 발자국이 거래소보다 비싸짐)이지만 (1) 전에는 추측이다. (3) **더 빠른 변수는 시장이 아니라 지갑이다** — `imp% ∝ √equity`이므로 지갑이 4배가 되면 종목이 그대로여도 impact가 2배다. 자본이 계속 늘면 capacity가 종목 선정보다 먼저 구속조건이 된다. 그때의 답은 종목 교체가 아니라 유닛 상한 / 종목 분산인데, 엔진은 심볼 하나를 배타 소유하므로 분산은 구조 변경이다.
- **`leg%`는 `atr%`와 거의 중복이다 (2026-09-01)**: 지그재그 임계가 `3 × ATR/px`라 다리는 구조적으로 임계의 ~1.8배가 되고, `leg%/atr%`가 7종목에서 **5.2~5.6**으로 거의 상수다(TRUMP 5.5로 평범). 즉 `concept ≈ 5.3 × legs/h × atr% × bounce × (1−ER)`이고, 지그재그 기계가 ATR 위에 얹어주는 횡단면 정보가 거의 없다. 후보는 `leg%`를 `atr%`로 대체(노브 제거는 이 데이터 예산에서 공짜다 — 측정 규약). 착수 조건: 종목 ≥ 15, 창 ≥ 3. 한 창 7종목으로 지우면 그게 과적합이다.
- **기각 (2026-09-01) — "굵은 눈금·넓은 스프레드가 큰 `leg%`로 위장된다"**: 세 추정량이 전부 반증한다. (1) `leg%/atr%`가 전 종목 5.2~5.6으로 TRUMP만 부푼 흔적이 없다. (2) 분산비 `VR15 = sd(15분 수익률) / (√15 × sd(1분 수익률))`는 마이크로구조 잡음이 많을수록 낮아지는데, TRUMP 0.87은 중간이고 최저는 **ZEC 0.77**(151M·스프레드 0.002%, 표에서 가장 유동적인 종목)이다 — 잡음과 유동성이 같이 가지 않는다. (3) Roll 추정 스프레드 `2√(−Cov(rₜ, rₜ₋₁))`는 TRUMP가 절대값 1위(0.069%)지만 자기 다리의 9%이고, 애초에 호가 튐과 **진짜 평균회귀**(이 전략이 먹는 바로 그것)를 구분하지 못해 근거가 못 된다. 다시 제안하지 않는다.
- PROMUSDT 덤프 국면 숏 백테스트, "하루 급락은 순환매 천국" 플래그 의미가 concept에 반영됐는지.
- **통행료 축이 이미 여기에 있다**: `concept = legs/h × leg% × bounce × (1 − ER)`의 `leg%`는 퍼센트이고, 통행료(왕복 0.048%)도 퍼센트다. 현재 랭킹에서 TRUMPUSDT leg% 1.20 / DOGEUSDT 0.26 — 같은 통행료에 대해 레그가 5배 차이 난다. 즉 "이 테이프가 통행료를 못 낸다"의 답은 CONCEPT상 쉬는 것이 아니라 **종목을 바꾸는 것**이고, 그 판단은 이미 select의 소관이다. 신호에 스케일 게이트를 넣자는 제안(1번)은 이 사실과 중복되지 않는지 먼저 확인한다.
- **벤치**: `bot.scan --json`, `bot.select` 로그, 후보 심볼 백테스트.

## 7. 운영 판단

- 튜너 `--apply` 승인 시점: 훈련 ≥ 2일 + 검증 1일이 처음 갖춰지는 날부터 리포트를 보고 키 단위로 승인. 격자에 "off" 값은 없다(메커니즘 스위치는 튜너가 만지지 않는다). 격자 탐색도 다중검정이므로 측정 규약의 3중 조건을 똑같이 진다.
- 백테스트 현실성: 청산가 가드·펀딩 없음(cap/명목 비율은 cap_frac 0.15로 live와 같아짐).

## 8. 포트폴리오 — 배분과 종목 구성

메커니즘은 RULES(포트폴리오 절)에 있다. 여기 남는 것은 **아직 정해지지 않은 것**뿐이다.

- **배분 규칙**: 지금은 균등(`wallet_frac` = 1/N)이다. 근거는 "어느 쪽이 나은지 모른다"는 것뿐이므로, live 관측이 쌓이면 `concept` 비례·성과 비례·균등 유지 중 무엇이 맞는지 판정한다. 판정 기준은 백테스트가 아니라 **심볼별 live 실적**이다 — 종목 질문이 백테스트로 안 풀린다는 것이 이 항목의 출발점이다(측정 규약).
- **종목 수와 구성을 언제 바꾸나**: 자주 바꾸면 이 항목의 목적이 무너진다. 각 심볼이 판정 가능한 표본을 쌓기 전에 갈아치우면 단일 종목 갈아타기의 문제(검증 시계 리셋)를 N배로 겪는 것이다. 재검토 주기와 교체 기준을 정해야 한다.
- **심볼별 일일 한도와 계좌 전체 한도**: `daily_loss_frac`이 `wallet_frac`으로 나뉘어 합은 보존되지만, 한 심볼이 자기 한도를 다 쓰고 멈춰도 나머지는 계속 돈다. 계좌 전체를 멈추는 층이 필요한지.
- **크로스 마진 결합**: 심볼들이 증거금 풀을 공유한다. 한쪽의 손실이 다른 쪽의 가용 증거금을 줄이고, 그때 `margin` 스킵으로 조용히 멈춘다. 이 결합이 실제로 문제가 되는지 관측 항목.
- **`select`의 미래**: 지금은 심볼이 둘 이상이면 물러선다. 배분기가 될지, 계속 세워둘지.
- **판정 도구**: `bot.cycles --symbol`, `bot.recon`(심볼별), 그리고 심볼별 live 실적의 짝지은 비교. 착수 조건: 심볼당 캠페인 ≥ 300.

## 결정된 것 — 다시 제안하지 않는다 (근거는 RULES)

테이커 담기 전환(저녁 +0.54 → −1.7) · ATR 스케일 레짐 스윙 임계(캐스케이드 바닥에서 켜짐, −16.3 → −20.3) · 3m EMA 진입 게이트(과적합) · 담기 TTL 90초 초과(300~1800초 전부 악화) · 레짐 거부권(반사이즈로 대체) · 구조 하드 스탑(B로 대체) · 담기 큐 대기의 시간축(큐가 원인) · **로트/캠페인 성적표에서 필터를 고르고 같은 표로 검증하는 것**(두 단위가 반대로 편향돼 있다 — 측정 규약).
