# C3 수리·실행 정밀감사 — 2026-09-05

최신 변경은 문서 마지막의 **C3 v3** 절이다. 앞의 v2 결과·등록 창은 당시 기록이며 v3 채택 판정으로 재사용하지 않는다.

이번 감사는 기존 계약의 준수 여부가 아니라 컨셉·통계 주장·실행·회계를 함께 검증한다. C2 감사의 결론도 증명이 아니라 당시 관측으로 취급한다. 동결 원본과 변경 전 소스는 `data/track_c/audit-c3-20260905/`에 보관한다. 이 디렉터리의 테이프와 9월 5일 기존 연구는 모두 개발 표본이다. 이번 수정의 채택 이유는 회계 항등식, 인과적 실행 가능성 및 재현 테스트이며 이 표본의 수익 최대화가 아니다.

## 변경 전 확인한 결함과 수정 계약

1. **잔량과 부의 보존**: 잔량 이월은 매도가 아니다. `W = 현금 + Σ(캠페인 수량 × 매수호가) + Σ(이월 수량 × 매수호가)`이고 `W − 초기자본 − 외부자금 = 실현손익 + 미실현손익`이다. 이월·병합 자체로 자본 또는 손익이 바뀌어서는 안 된다. C3의 기존 equity·일손실·알림에서 residuals가 빠져 있었다. 수정: 잔량의 원가·mark·평가시각을 유지하고 위험과 자본에 포함한다. 거래할 수 없는 잔량의 원금은 무보호 노출로 예약하되, 같은 종목 병합 진입은 병합 후 계획 위험으로 다시 심사한다. 표본 끝의 잔량은 승리 또는 손실 0의 종료가 아니다.
2. **첫 매수체결 후 보호 지연**: 기존 OMS는 C3를 quantitative로 분류하여 매도가 가능한 부분체결에도 잔여 매수를 TTL까지 유지했다. 그 동안 익절 주문은 제출 불가했다. 수정: stop_limit에서도 최소 매도가 가능한 부분체결이면 잔여 매수를 취소·재조회한 뒤 실제 확정 잔량의 익절 주문을 낸다. 매도 불가능한 작은 체결은 기존 주문의 범위에서만 기다린다. 취소 중 추가체결은 누적 차분으로 반영한다.
3. **고정된 위험 계획**: 틱 구간 경계에서 현재 tick × 3은 유효 호가 세 단계와 다르다. 청산 때 현재 tick으로 손절선을 재계산하면 진입 계획의 위험도 바뀐다. 수정: 유효 격자를 걷고 진입 때 고정한 stop을 사용한다. 유한한 가격·수량·편차·가중치, 최소 수량 및 최대 주문액을 검증한다. 계획 손절액은 소프트웨어/시장가 지연·갭 손실의 보장 상한이 아니다.
4. **연결과 데이터 품질**: 마지막 호가의 나이는 연결 생존의 증명이 아니다. 선행 연결 해제는 즉시 공정가 기여를 무효화하고 재연결 후 새 호가가 필요하다. 연결 상태를 녹화한다. 과거 녹화의 연결 정보 부재는 재생 한계로 드러낸다. NaN/Inf와 역순 호가를 거절한다.
5. **재생과 추론**: 기존 재생기는 신규 진입 종료 시점에 기존 재고까지 강제청산했고, 실제 매수금액 대신 주문 예정금액으로 bp를 나눴으며, 잔량 평가손익·완전한 일별 수익·비용 스트레스·채택 판정을 제공하지 않았다. 수정: 진입 종료와 최종 청산을 분리하고 실제 체결금액, 잔량 포함 총자산, 최대낙폭, 비용 스트레스, 시각·품질·코드/데이터 식별자를 보고한다. 캠페인은 독립 표본으로 간주하지 않는다.
6. **주문 직전 지연**: C2에서 지적한 계정 REST 재조회가 C3에도 매 후보마다 남았다. 직렬 장부와 5초 이내 계정 스냅샷을 재사용하고 기존 주기 동기화·미확정 주문 소유권·실제 가용잔고 검사를 유지한다. 사소한 지연 절감은 수익성 증거와 구분한다.

## 수리적 판정 기준

`E[P(t+h)−P(t) | dev] > 0`은 `E[매도대금−매수대금−비용 | 주문이 체결됨] > 0`을 함의하지 않는다. 주문 체결은 미래 가격 경로에 의존하는 선택 사건이다. 편차의 예측력, 체결 확률, 체결 조건부 가격 변화, 체결 후 청산 정책을 함께 평가해야 한다. `dev ≥ 0`이 역선택을 제거한다는 문장은 철회한다.

잔량가중 중간가 `(ask·bid_qty+bid·ask_qty)/(bid_qty+ask_qty)`는 관측량이다. 별도의 전이모형을 추정하는 Stoikov의 micro-price나 조건부 기댓값이라는 등식은 성립하지 않는다. 단기 후행 상관만으로 공적분, 코인원의 공통요인 가중치 0, VECM 또는 시장 효율가격을 확정할 수 없다. 공정가는 이름을 유지하되 **거래소 간 가격 기준의 휴리스틱**으로 명시한다. 마팅게일 가격에서도 평균회귀하는 호가 오차와 비동기 관측 때문에 후행 상관이 생길 수 있다.

기존 fairfit은 시차 차분항 없는 축약 회귀이며, rolling median 프리미엄을 빼서 안정적으로 보이는 편차를 만들었다. 그것은 공적분 검정이 아니다. 두 가격 차이의 동학을 근사하더라도 조정속도는 `alpha_coinone − alpha_leader`이며 이산시간 반감기는 `log(.5)/log(1+alpha_coinone−alpha_leader)`다. 코인원 계수 하나로 계산한 반감기와 그 회귀에서 만든 정보지분은 가중치 선택에 사용할 수 없다.

평가의 주 지표는 고정 운용자본의 **잔량 포함 순자산 변화/시간과 최대낙폭**, 보조 지표는 실제 체결금액당 순손익이다. 1틱 이득·3틱 손실만 발생하는 이상화에서는 비용 0이어도 승률이 75%를 넘어야 한다. 실제 방어·시간·부분체결의 손익 분포를 이 이항모형으로 대체하지 않는다. 이전 문서의 수익률 상한·역선택 제거·모든 시점 음수 같은 보편 명제는 작은 표본으로 입증되지 않았다.

## 과적합 방지와 운영 범위

현재 1만 원 실행 표본 수집은 유지한다. 이번 감사는 종목·진입 문턱·손절 틱·목표 틱·운용 크기를 테이프 결과에 맞춰 최적화하지 않는다. 모든 수정은 전후 동일 테이프/지연/대조군으로 검증한다. 9월 5일 이후 처음 보는 완전한 KST 일들을 고정 버전으로 수집하고, 결과를 본 뒤 변형을 선택하면 그 이후 새로운 평가 구간이 필요하다. 5일 중 4일 양수는 공정한 독립 부호 검정에서도 단측 p=0.1875이므로 90% 우위 증명이 아니다. 블록 구간은 자기상관·비정상성 아래의 근사이며 미래 수익 보장이 아니다.

배포는 테스트와 재생이 끝난 C 코드만 새 release에 준비하고, 신규 진입 일시정지 후 장부와 실제 C 주문의 flat을 같은 작업 안에서 확인하여 live 설정과 최신 장부를 보존한다. 현재 주문액·위험 한도는 유지한다. 오래된 장부 복원, A/B 재개 또는 학습 worker 재개는 하지 않는다.

## 근거 문헌

- [Stoikov, The Micro-Price (원저자 논문)](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2970694): weighted mid와 micro-price의 구분.
- [Bailey et al., The Probability of Backtest Overfitting](https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf): 전략 반복 선택에 의한 표본외 성능 왜곡.
- [코인원 주문 정보 조회](https://docs.coinone.co.kr/v1.1/reference/order-detail): 누적 체결·취소·잔량의 구분.

## 측정 결과와 판정

**현재 판정은 수익성 미입증, 증액 보류다.** 수리·회계·실행 결함은 수정하지만 신호의 양의 기대수익이 증명된 것은 아니다. 단일 가격 대기열에 더 좋은 가격의 호가까지 합산하던 재생 결함도 수정했다. 기존 재생기의 결과와 수정 재생기의 결과를 바로 빼서 전략 개선량으로 보고하지 않는다.

추가 개발 구간은 **2026-09-05 21:00:00.371–21:57:00.471 KST**다. 같은 초기자본 594,574원, 결정 간격 500ms, 체결 지연 250ms, BTC/ETH/XRP/SOL로 비교했다. 원본은 Coinone 98,723줄, 선행거래소 278,709줄이며 마감 시각은 21:57:00.514 KST다. 쓰는 중이던 선행 gzip의 미완료 꼬리는 완전한 JSON 줄만 별도 재압축했으며 이를 `remote-manifest.json`에 기록했다. 4개 거래 종목에서 중복·역순 체결 26건과 늦거나 낡은 관측 12건을 거절했다. 과거 테이프에는 연결 단절 이벤트가 없어 연속 수집을 증명하지 못한다.

| 동일한 수정 재생기에서 평가한 정책 | 주문 시도 | 종료 기록 | 잔량 포함 순자산 변화 | 최대낙폭 |
|---|---:|---:|---:|---:|
| 동결한 기존 C3 실행·위험 규칙 | 126 | 21 | −19.2831원 | 21.2164원 |
| 수정 C3 v2 | 105 | 20 | −18.0415원 | 23.6963원 |
| 편차 방향 반전 대조군 | 122 | 41 | −77.1189원 | 82.2922원 |
| 편차 진입·취소·방어를 제거한 대조군 | 101 | 40 | −18.6743원 | 43.2617원 |

종료 기록 수에는 이월이 포함돼 독립 왕복거래 수나 유효 표본 수와 같지 않다. 기본 결과의 BTC 잔량 0.00001966개(원가 2,141.5638원)는 자산으로 남았고, 부의 보존 오차는 0.000001원 미만이다. 기본 정책의 순자산 변화는 기존보다 **1.2416원 높고 최대낙폭은 2.4799원 커졌다**. 무조건 대조군 대비 차이도 0.6328원에 불과하다. 이 결과는 수익 우위나 위험조정 성과 개선의 증거로 충분하지 않다.

기존 재생기는 같은 추가 구간에서 122시도/14종료/실현 −23.6944원을 보고했다. 이는 강제청산 시점·대기열·분모·잔량 회계가 다른 참고치다. 최초 연구의 15:17:23–15:47:23 KST 구간을 수정 경로로 돌리면 107시도/9종료/순자산 +4.9993원이지만, 양방향 거래금액에 **편도 0.5bp**의 추가 비용만 적용해도 −3.0012원이다. 기존 30분 흑자를 일반적인 기대수익으로 해석할 수 없다.

추가 구간의 수정 기본 정책에 편도 0.5/1/2bp를 더한 순자산 변화는 각각 **−36.1486/−54.2556/−90.4697원**이다. 지연 1,000ms의 별도 스트레스는 −15.9680원, 최대낙폭 21.7549원이었다. 지연이 커졌을 때 손실이 줄어든 한 표본은 지연이 유리하다는 뜻이 아니다. 대기열과 체결 선택이 바뀌었기 때문이다. 지연이나 비용 조건을 결과에 맞춰 선택하지 않는다.

최초 동결 실장부의 C3 표본은 193시도/3완료/실현 +6.179233원이었다. 후속 실거래가 계속됐으므로 현재 잔고·손익과 혼동하지 않는다. 이 작은 실체결 표본으로 재생기의 체결 가정이나 양의 기대수익을 검증할 수 없다.

### 후행 상관의 반례

효율가격에 선행 정보가 없고 호가 오차 `e_c,e_l,e_f`와 이후 효율가격 변화 `u`가 서로 독립인 경우를 보자. 관측 편차 `X=e_l−e_c`, 이후 코인원 수익 `Y=u+e_f−e_c`라 놓으면 `Cov(X,Y)=Var(e_c)>0`이다. 네 변수를 표준정규로 두면 `Corr(X,Y)=1/√6≈0.4082`다. seed 20260905, 100,000개 합성 표본에서 0.4043이 재현됐다(`quote-noise-null.json`). 공유된 현재 코인원 호가 오차만으로도 양의 상관이 생긴다. 이 반례는 실제 시장에 선행성이 없다는 증명이 아니라, 관측 상관만으로 선행성을 식별할 수 없다는 증명이다.

### 고정된 이후 평가

`evaluation-c3.json`은 **9월 6일 00:00부터 9월 16일 00:00 KST 직전까지 10일**을 고정한다. 9월 5일은 전부 개발 표본으로 제외한다. 코드·설정·체결 시나리오 식별자, 기본/반전/무조건 대조군, 무거래일을 포함한 일별 순자산을 함께 동결한다. 워밍업은 평가 시작 전 데이터를 사용하고 끝의 청산 여유는 평가 종료 후 데이터로 확보한다. 첫 10일은 정해진 점검 시점이지 수학적으로 충분한 표본 수라는 주장이 아니다.

`c3_evidence`는 순수익 및 두 대조군과의 일별 차이 세 주장에 고정 2일 원형 블록 부트스트랩(20,000회, seed 20260905)과 명목 family alpha 0.10/3을 적용한다. 독립 부호 검정은 평균 수익 검정과 구분한다. 시각·코드·설정 불일치, 미완료 창, 미해결 포지션·회계 오류가 있으면 HOLD다. 통과도 REVIEW_ONLY이며 자동 증액·자동 채택은 없다. 데이터 공백, 잔량 가격의 신선도, 실제 체결과의 일치, 1초 지연·비용 스트레스는 별도로 검토한다. 결과를 보고 규칙이나 구간을 바꾸면 새 미래 평가가 필요하다.

### 구현 검증과 운영 반영

회계 보존·부분체결·취소 경합·틱 경계·비유한 관측·연결 무효화·통계 판정 등의 회귀 테스트를 포함해 **C 127개, bot 300개, quant 59개가 통과**했다. 서버에서도 C 전체 및 독립 알림 테스트를 통과했다. 수익 수치를 맞추기 위한 문턱 탐색이나 종목 재선택은 하지 않았다.

엔진은 flat·실제 C 주문 0 확인 후 `20260905-230639-c9d8d2478773`으로 전환했다. 기존 live config의 해시를 교체 전후 대조하고 최신 장부를 보존했다. 네 종목 워밍업 후 자체 PAUSE를 제거했고 **23:10:09 KST 최종 확인**에서 live·공개/개인/두 선행 연결 정상, 재시작 0, HALT·API 오류 없음이었다. 매매·평가 의존 파일 27개의 서버/등록 해시가 일치했다. 이후 첫 ETH 왕복은 0.00298063개, 순손익 +2.98063원으로 정리됐고 진입 알림 0건·전량청산 알림 1건을 확인했다. 이는 실행과 알림 검증이며 수익성 검증이 아니다. 영수증은 `logs/track-c/c3-upgrade-staged.json`, `logs/track-c/c3-audit-final-verification.json`이다. 별도 알림은 `20260905-230533-bd7b5358c7eb`으로 갱신했고 이 작업 때 엔진 PID는 바뀌지 않았다.

미래 평가 등록 사본은 서버 `data/evaluation/c3-v2-20260906.json`이며 SHA-256은 `1013d739c45ad639ae1c6b036e52e3f768a343c321c9278a44165f53977e0c57`이다. 현재 개발 결과를 넣은 등록 검증은 소스·설정·대조군 일치, 완전한 미래 일수 0/10으로 HOLD를 반환했다. 측정 원본·코드·중간 결과의 위치는 `data/track_c/audit-c3-20260905/manifest.json`, 최종 결과는 `final-{base,flip,unconditional}-250.json`이다.

사용자 추가 지시에 따라 **진입 알림은 끄고 청산 체결만 발송**한다. 매수 체결은 회계·캠페인 통계에 계속 반영한다. 기존 대기 큐의 진입도 발송하지 않으며, 매도 없이 잔량만 이월한 사건은 청산 알림으로 보내지 않는다. 운영 이상·복구·정기 상태 알림은 기존 계약을 유지한다.

## C3 v3 — 상대가치·선행 가격 추세·공격 체결 흐름 분리

사용자가 제시한 실체결 41건의 개발 관측은 다음과 같다. 표기 구간은 21:59–23:30 KST이며 **사용자 제공 집계**다. 이번 세션이 동일 정의로 전체 원장을 재계산한 통계가 아니다. 아래 부분집합을 합쳐 독립 표본이나 41건 전체의 완전한 분할로 취급하지 않는다.

| 사용자 제공 분류 | 건수 | 손익 | 익절 / 손절 |
|---|---:|---:|---:|
| 진입 전 업비트 30초 수익률 < −1bp | 9 | −3.4bp/건 | 미제공 |
| 업비트 30초 상승 | 4 | +1.3bp/건 | 미제공 |
| 코인원 32초 공격 체결 매수우세 | 17 | −49.6원 | 4 / 7 |
| 코인원 32초 공격 체결 매도우세 | 15 | −11원 | 10 / 2 |
| 손절 중 BTC, 진입 편차 1.0–2.1틱 | 4 (전체 손절 9 중) | 미제공 | — / 4 |

편차는 추세와 다르다. 다만 고정 tick에서 `Δdev=(ΔF−Δmid)/tick`이므로, **공정가가 하락했다는 사실만으로 양의 편차가 커지지는 않는다**. 코인원이 더 빠르게 하락하는 등 상대 변화가 필요하다. 업비트 수익률(bp)에 관한 관측과 구현하는 합성 공정가 변화(틱)도 서로 다른 측정량이다. 위 집계는 v3 가설의 개발 동기이며 식별된 인과효과나 양의 기대수익 증거가 아니다.

구현 규칙은 사용자 지정대로 고정했다. A는 기존 편차 하한에 더해 공정가가 익절가 이상이어야 한다. B는 `m30≤−1`과 `m10≤0.3333×m30`의 하락 지속을 veto하고, 첫 체결 후 같은 조건이면 `brake`로 청산한다. 감속 중에는 브레이크를 적용하지 않으며 기존 손절·방어·시간 조건은 유지한다. C는 코인원 32초 공격 체결의 금액 가중 순매수 비중 `(매수−매도)/(매수+매도)≤0`이다. 무체결은 기존 feature 정의대로 0이다. 결측·비유한 flow는 거부한다.

`F(t−30초)`·`F(t−10초)`는 1Hz 이력에서 해당 시각 **이전의 최신 표본**을 사용한다. 현재 tick으로 나누고, 기여 거래소 집합 변경·알려진 연결 단절·이력 공백에는 초기화한다. 모멘텀 불가 시 신규/대기 진입은 거부하지만 그 이유만으로 기존 보유를 시장가 청산하지 않는다. 구성 변경에 따른 공정가 점프를 모멘텀으로 사용하지 않는다. 0.3333은 요청한 설정값이며 정확한 1/3의 유한 소수 근사다.

### v2 대 v3 스모크 — 6개 고정 재생

서버 공개/선행 테이프 `20260905-12/13/14.jsonl.gz`를 **23:17:30.043 KST**에 함께 동결했다. 14 UTC 파일은 작성 중이므로 완전한 JSON 줄까지만 별도 재압축했다. 공개 241,724줄, 선행 900,081줄, 계약 캡처 909개다. 데이터와 v2 소스(`3e8c0f8`)는 `data/track_c/audit-c3-v3-20260905/`에 보존했다. 동일 초기자본·250ms 지연·500ms 결정 간격·종목·크기·손절 조건으로 재생했다.

| 버전 / 대조군 | 주문 시도 | 체결 캠페인 | 캠페인당 실현 손익 | 평균 bp/캠페인 | 손절 | 브레이크 | 잔량 포함 순자산 변화 |
|---|---:|---:|---:|---:|---:|---:|---:|
| v2 기본 | 418 | 75 | −1.6958원 | −1.6913 | 14 | 0 | −127.1834원 |
| v3 기본 | 278 | 34 | −0.7438원 | −0.7337 | 4 | 14 | −25.2879원 |
| v2 flip | 440 | 143 | −2.5595원 | −2.5881 | 23 | 0 | −366.0085원 |
| v3 flip | 236 | 83 | −1.5835원 | −1.5898 | 6 | 14 | −131.4327원 |
| v2 unconditional | 370 | 135 | −1.6773원 | −1.6772 | 35 | 0 | −226.4303원 |
| v3 unconditional | 370 | 135 | −1.6773원 | −1.6772 | 35 | 0 | −226.4303원 |

기본 체결 캠페인은 **54.7% 감소**했다. 부분체결 메시지 수가 아니라 재생의 체결 캠페인 수이며, 이월 종료도 포함하므로 독립 표본 수가 아니다. 기본 최대낙폭은 151.5130→34.9681원이었다. unconditional의 결과가 두 버전에서 정확히 같아 새 신호 조건을 끈 비교에서 기존 실행 조건이 유지됨을 확인했다. **모든 총손익은 음수이며 이 결과를 채택·증액 근거로 사용하지 않는다.** 결과를 본 뒤 임계값·종목·손절·크기를 조정하지 않았다.

### 검증·전환·이후 평가

v2는 테스트 127개 통과·실제 배포를 확인한 후 `3e8c0f8`로 보존했다. v3는 veto·감속 예외·브레이크·A/C 게이트·결측·틱 경계·거래소 집합 변경·과거 시각 참조·설정 격리·전환 이전 손익 제외를 포함해 C 테스트 **139개 통과**, 서버 C/알림 테스트 통과 후 배포했다. 새 설정은 `momentum_window_s=30`, `momentum_recent_s=10`, `momentum_veto_ticks=1`, `momentum_decel_share=0.3333`, `flow_gate=true` 다섯 개뿐이다.

`prepare→switch→resume`에서 최신 장부와 나머지 live 설정을 보존한다. 워밍업을 마친 재개 시각을 v3 평가 시작으로 고정하고, `notifications/baseline.json`의 같은 날 `start_ms`도 그 시각으로 옮긴다. 이는 표시 기준선이며 원본 장부·실현손익·위험 예산을 초기화하지 않는다. 기존 진입 알림 OFF와 청산 전송은 유지하고 `brake` 사유를 표시한다.

평가 파일은 전환 시각부터 고정 **10×24시간**으로 새로 등록한다. KST 자정 이전 v2 손익이 섞이지 않도록 재생은 시작 전 워밍업만 하고, 추론은 전환 시각에 고정한 24시간 순자산 블록을 쓴다. 기존 2블록 근사·대조군·HOLD/REVIEW_ONLY·자동 증액 금지를 유지한다. 종전 v2 등록은 서버와 커밋에 역사로 남긴다.

실제 전환: 엔진 `20260905-232641-2904c5801092`, 알림 `20260905-232634-8b3815f486be`. flat·실제 C 주문0 확인 뒤 엔진을 교체했고 모멘텀까지 워밍업한 **2026-09-05 23:30:00.029 KST**에 자체 PAUSE를 제거했다. 평가 종료는 9월15일23:30:00.029KST(미포함)다. `resumed_at_ms=1788618600029`를 평가와 알림 기준선에 함께 사용했다. 알림 기준선 반영 직후 표시 손익0원·수익률0.00%·캠페인0회·승률집계전을 확인했다. 기존 장부와 위험 손실 누적은 초기화하지 않았다.

서버 등록 `data/evaluation/c3-v3-1788618600029.json`, SHA-256 `a0bf73ba007e70b3d41227743555ac07c3850cd780b654093f50bb4cb254089e`. 매매·평가 소스27개 해시와 등록이 일치하며 live 설정은 승인된5개 이외 전부 동일함을 재확인했다. 영수증은 `logs/track-c/c3-upgrade-staged.json`, `logs/track-c/c3-v3-finalization.json`이다. 원본/최종 결과 파일은 `data/track_c/audit-c3-v3-20260905/{v2,v3}-{base,flip,unconditional}.json`에 있다.

23:33:35 KST 실거래 확인에서 재시작0·HALT/저장/연결 장애 없음, 신규 `RULE_DECISION` 13개의 A/B/C 충족과 m30/m10/flow32 기록을 검증했다. 첫 XRP 왕복은 **brake 청산 −5.17063082원**, 진입 알림0·전량청산 알림1이었다. 표시 계기판은 v3만 **1캠페인/−5원/0승1종료**였고 기존 실장부 누적 −104.42744042826원은 보존돼 이전 손익이 표시에서 제외됨을 확인했다. 이는 브레이크·알림·기준선의 실행 검증이며 수익성 증거가 아니다. `logs/track-c/c3-v3-health-latest.json` 참조. 개발 스모크를 새 평가 파일에 넣은 검증도 전환 창 불일치·미완료0/10으로 HOLD를 반환했다.

## BTC 단독으로 검증 범위 변경 — 2026-09-06

사용자가 `coins=["BTC"]`, ETH·XRP·SOL의 `record_coins` 이동, 나머지 파라미터 유지, flat에서 `c3_upgrade` 전환, 평가·알림 기준선 재시작 및 Slack BTC 통계만 집계를 명시적으로 승인했다. 기존 16개 기록 종목을 보존하므로 전체 녹화 대상은 동일한 20종목이다. C3 규칙 버전은 `c3-rule-v3`이며 새 수익 가설의 문턱을 추가하지 않는다.

제공된 근거의 수학적 범위를 바로잡는다. 편차 표준편차/틱(BTC 1.41, ETH 0.74, XRP 0.57, SOL 0.40)은 **사용자 제공 수치**이며 이번 작업에서 재추정하지 않았다. 표준편차가 1틱 미만이라는 사실은 진입 A가 불가능함을 뜻하지 않는다. `F-bid = (dev + spread_ticks/2)*tick`이므로 1틱 스프레드에서는 `dev≥0.5`도 A를 만족하고, 2틱 스프레드에서는 기존 편차 하한 0으로 A를 만족할 수 있다. 분포의 표준편차만으로 그 꼬리 확률·평균·진입 가능성을 결정할 수도 없다. BTC만 A가 성립한다는 주장은 기록하지 않는다.

같은 명목 1만원·3틱 손절도 종목별 비율 위험이 다르다는 비교 한계는 유효하다. 앞선 동결 테이프의 명목 손절은 BTC 약2.75bp, ETH8.93bp, XRP15.53bp, SOL21.34bp였다. 원화 성과 기여도가 달라지므로 종목별 승패만으로 위험을 맞춘 우열을 판단할 수 없다. 다만 XRP·SOL 손익을 통계적으로 잡음이라고 확정한 것도 아니다. 이번 결정은 BTC 단독 미래 검증이며 이전 성과를 본 뒤 선택했다는 한계가 남는다.

구현은 `c3_upgrade prepare --profile btc-only`에서 두 종목 설정만 교체하도록 범위를 고정하고, 녹화 대상의 합집합 보존·중복·BTC 기록 전용 중복을 확인한다. switch는 자체 PAUSE 후 기존 캠페인/주문과 실제 C 주문이 없는 상태에서만 중단·교체한다. resume은 연결·공정가·30/10초 이력을 검증한 뒤 동일 시작 시각의 평가 등록과 BTC 알림 기준선을 먼저 영속 저장하고 PAUSE를 제거한다. 원본 장부·실제 자본·UTC 손실 예산은 초기화하지 않는다.

알림은 전체 이벤트를 재생해 교차 종목 주문 문맥을 유지한 뒤 BTC 체결의 손익·첫 체결 횟수·완료 승률만 합산한다. 종목 필터는 날짜가 바뀌어도 유지하고 시간 기준선만 해당 KST 일자에 적용한다. 잔고·가용·수익률 분모는 실제 운용자본이다. BTC 외 녹화 종목을 매매 종목으로 표시하지 않으며 진입 알림OFF/청산 알림 유지다.

검증: 로컬 Track C 144개 및 공통 알림 4개, 서버 Track C 144개 및 독립 알림 29개 테스트 통과. 추가 검증은 다른 설정의 불변, 녹화 대상 누락 거부, 평가 10×24시간·시나리오/부트스트랩 보존, 알트와 BTC 교차 체결의 통계 분리 및 자정 이후 필터 유지다. 이번 종목 변경의 성과를 보고 문턱을 조정하는 재생은 실행하지 않았다.

실제 배포: 엔진 `20260906-000144-2b4ab6b88980`, 알림 `20260906-000155-a8f998c7a081`. 첫 switch는 XRP 캠페인이 남아 대기했고, 기존 규칙으로 종료된 뒤 두 번째 switch에서 flat과 실제 C주문0을 확인했다. BTC 기준가격·모멘텀 워밍업 후 **2026-09-06 00:06:30.977 KST (`1788620790977`)**에 재개했다. 종료는 **2026-09-16 00:06:30.977 KST 미포함**이다. 기존 평가 사본 `evaluations/c3-v3-1788618600029.json`은 보존했다.

서버 등록 `data/evaluation/c3-v3-btc-1788620790977.json`, SHA256 `db639a96734ba168514e3bc6bdf858eb549ae7671cb1a7d52e46ba60923567fd`. 27개 매매·평가 소스 해시와 파라미터가 일치한다. live 설정 차이가 두 키뿐임과 교체 전 장부 이벤트 **34,473건의 완전 일치**를 검증했다. 이월 잔량0, 모델 worker/timer 및 독립 리더 recorder 미기동, A/B 미변경이다. 재개 직후 표시 손익0원/수익률0.00%/캠페인0/승률집계전을 확인했고 알림 장애·미발송 큐0이었다. 실제 카드도 매매 종목BTC만 표시한다. 영수증은 `logs/track-c/c3-btc-only-health.json`, `c3-btc-only-card-preview.json`, `c3-upgrade-staged.json`이다.

후속 표시 요청: 손익의 소수점 첫째 자리 표시를 체결·캠페인·누적·미실현에 적용했다. 반올림은 출력 시점에만 하며 통계 집계·승패 판단·장부를 반올림하지 않는다. 알림 release `20260906-001037-7718e3a6c6a1`만 갱신하고 엔진PID16303·평가/기준선 시작시각을 유지했다. 관련 로컬/서버 알림29 tests PASS, 실제 BTC 청산과 누적 손익 `-1.8원`, 양수 `+0.9원`, 음수 반올림 `-2.8원`, 음의0 제거 `+0.0원`을 서버 렌더러에서 확인했다. `logs/track-c/c3-pnl-decimal-health.json` 참조.

## 사용자 지정 진입 기본값2틱·슬랙 편차 필터 — 2026-09-06

00:45:21KST 동결 실체결에서 진입 주문 당시 편차 `[1,2)`와 `[2,3)`를 비교했다. v3 전체는7승/10건 대4승/4건(단측 Fisher p=0.330), BTC 단독 전환 후는1승/4건 대1승/1건(p=0.400)이었다. 2틱 이상3틱 미만 외3틱 이상 완료 사례는 없었다. 우위는 유의하지 않고 시간대·체결 선택·자기상관을 보정한 인과 추정도 아니다. 계산과 원자료 식별은 `logs/track-c/btc-deviation-study-20260906.md`에 보존한다.

이 결과를 본 뒤 사용자가 **최적값이 아닌 기본값으로 2틱을 사용**하라고 명시했다. 기존 '결과를 보고 임계값을 바꾸지 않는다'의 이번 예외는 사용자의 새 지시다. 기본값이라는 명칭이 선택 편향을 제거하지 않으므로 이전 관측은 새 설정의 검증 표본으로 재사용하지 않는다. 추가 문턱 탐색이나 성과를 보고 반복 조정하는 재생은 하지 않았다.

계약은 신규 주문 시 `dev=(F−mid)/tick ≥ 2`, 상한 없음이다. `entry_ticks:0.0→2.0`만 바꾸고 취소/방어−0.5틱·익절1틱·손절3틱·보유180초·TTL60초·1만원·모멘텀/flow·종목·위험 설정은 보존했다. 대기 주문이 나중에2틱 미만이 돼도 그것만으로 취소하지 않으며, 이번 통계 역시 첫 체결 시점이 아닌 주문 의도 당시 편차다.

`prepare --profile entry-2 → switch → resume` 완료. 엔진 release `20260906-005506-272bfcbfc659`, PID18638, 재개 **2026-09-06 00:57:27.869KST (`1788623847869`)**, 평가 종료9월16일 같은 시각 미포함이다. 설정 diff1키, 등록된 매매·평가 소스27개 동일, flat/실제 C주문0/원본 이벤트36,089개 보존을 확인했다. 로컬/서버C146 테스트와1.999999 거부·2.0 허용·3.0 허용 경계 스모크 통과. 서버 테스트에 없는 평가 파일을 읽던 새 테스트의 이식 문제는 고정 fixture로 수정한 뒤 다시 통과했다. 첫 prepare 실패 때는 live를 교체하지 않았다.

이전 BTC 평가 `evaluations/c3-v3-1788620790977.json`과 서버 불변 등록은 보존했다. 새 서버 등록 `data/evaluation/c3-v3-btc-entry-2-1788623847869.json`, 로컬/서버 SHA256 `260441d898ce6124b79cbb46d965b07d4c5d5f9cdce68c82dee5c988dde1964a`. 시나리오·10일·부트스트랩·대조군·자동증액 금지는 불변이다.

resume 직후 표시도 새 시각으로 시작했지만, 후속 사용자 지시 **'기존 통계에서2틱 아래 제외, 데이터가 아니라 슬랙에서'**에 따라 표시 시작은 기존 BTC00:06:30.977KST로 복원하고 `entry_dev_min_ticks=2.0`을 설정했다. 평가 창은00:57:27.869KST 그대로다. 전체 장부를 읽어 원래 캠페인 편차에 모든 부분체결·수수료를 연결하고, BTC 및 편차2틱 이상 캠페인의 손익/분자/횟수/승률만 집계한다. 미확인 편차는 포함하지 않고 필터는 자정에도 유지한다. 잔고·가용·수익률 분모는 실제 자본이며 원본 이벤트36,118개가 표시 필터 적용 전후 동일했다.

알림 release `20260906-005913-53649994640e`, PID19191만 교체하고 엔진PID18638·설정·평가 파일은 그대로였다. 관련 로컬/서버31 테스트 통과. 확인 당시 기존8완료 중 편차2.4125틱1건만 포함돼 **1캠페인·1승·100.0%·+0.9원**이었다. 이는 과거 데이터를 필터한 기술통계이며 새 설정의 전향 성과가 아니다. 이미 발송한 Slack 메시지는 수정하지 않고 이후 카드부터 적용한다. `logs/track-c/c3-entry-two-health.json`, `c3-slack-deviation-filter.json` 참조.
