# C3 운영·실행 개선 — 2026-09-06

사용자가 `logs/track-c/review-c3-20260906.md`의 개선 항목 전부 수정을 승인했다. 수리 시작 시 기준 커밋은 `5ad37f8`, 당시 실운영은 BTC·진입 편차2틱·1만원·익절1틱·손절3틱·TTL60초·보유180초다. A/B·C2를 재개하지 않는다.

## 구현 계약

후속 사용자 승인과 병행 세션의 구현을 통합한다: 기준 주문금액2만원, 진입 시 변동성으로 계산해 고정하는 손절, 남은 보유 가치 청산을 적용한다. 최소 청산금액105%를 충족하도록 목표 수량을 올릴 수 있으나 기준 수량·현금·포트폴리오 위험 한도는 지킨다. 세부 계산과 한계는 `EXIT-C3-20260906.md`를 따른다. 종전1만원·고정3틱·증액 금지 제약은 이 승인으로 대체한다. BTC/진입2틱/익절1틱은 유지한다.

1. 루트 EBS를 실제 인스턴스·NVMe serial까지 대조한 뒤 8→32GiB 온라인 확장한다. 기존 데이터는 보존한다. C public 녹화 한도를16GiB로 늘리고 public/leader 증가율·잔여 가능 시간·10일 소요 공간을 상태에 기록한다. 실제 공간1GiB 보호선은 유지한다.
2. 선행거래소 구독 목록을 주기적으로 재확인하되 같은 집합이면 연결을 유지한다. 실제 구독 집합 변경/단절의 가격·모멘텀 초기화는 유지한다.
3. C 전용 HTTPS 연결 풀을 사용한다. 요청 자동 재시도·리다이렉트는 금지하고 응답 유실은 기존 주문 의도로 재조회한다. 청산 단계를 측정하며 시세 변화가 보유 판단을 깨운다. 취소 후 재조회와 가용 수량 확인은 생략하지 않는다.
4. systemd가 주 루프의 생존 신호를 감시한다. 비정상 종료 뒤 단일 writer를 인수한 복구 작업은 C 주문을 재조회·취소하고 매도 가능한 소유 수량에 고정된 STOP_LIMIT 보호를 남긴다. 정상 엔진은 시세/공정가 워밍업 뒤 보호 주문을 취소·재조회하고 익절 호가 운용으로 복귀한다. 시스템 전체 정지·거래소/API 단절·갭에서 손실 상한을 보장한다고 표현하지 않는다.
5. C3는 A/B/C2 신호를 중복 계산하지 않는다. 호가/체결 원본과 기존 규칙이 사용하는 flow32를 보존한다. 연구용 기존 경로는 호환성을 유지한다.
6. 재생은 종목 필터 후 시간순 스트리밍 입력과 제한된 체결 대기열 상태를 사용한다. 캠페인·위험·공정가 이력은 시간 파일 경계를 넘어 유지한다. 순자산 블록/낙폭/회계 검증은 전체 시계열을 메모리에 유지하지 않고 계산한다.
7. 수익 우위를 만들어 냈다고 주장하지 않는다. 실제 체결의 시도/완료/부분체결, 체결 후 가격 변화, 비용·지연 스트레스와 재생 대조를 읽기 전용 보고로 구현한다. 이 분석 결과에 따른 자동 증액과 신호 문턱 탐색은 하지 않는다.

## 전환 규약

엔진 동작과 소스가 바뀌므로 이전 전향 평가를 보존하고 새 실행 버전의 재개 시각부터10×24시간을 등록한다. 기존 Slack 표시 시작00:06:30.977KST와 BTC/진입편차2틱 필터는 그대로 보존하며 평가 시작과 다시 맞추지 않는다. 원본 장부·자본·UTC 위험 누적은 유지한다. `c3_upgrade`의 prepare→PAUSE→flat·실제 C주문0→switch→워밍업→resume을 사용한다.

## 통합 검증

- 2026-09-06 02:33 KST 로컬 C 전체205 테스트 통과. 응답 유실 무재시도, 취소 중 체결, 미체결과 기존 잔량 복원, 보호 주문의 워밍업·퇴역, 급락이 매수 체결 확인보다 앞선 경우, 시세 두 시계의 신선도와 잔량을 남긴 CLOSE 집계를 확인했다. Slack 테스트의 HTTP 응답은 모의 응답이다.
- 동일 고정 테이프의 재생 전후4개(base/flip/unconditional 및1초 지연)에서 시도·체결·순자산·잔량·위험 결과가 일치했다. 전체 시장 이벤트와 순자산 곡선을 메모리에 보존하지 않는다. 137,425개 이벤트의 임시 디스크는27.3MB, 한 스텝 최대51개·호가 최대6개였다. 시간순 압축 spool은 전체 입력 정렬을 위한 디스크를 쓰므로 저장공간이 충분해야 한다.
- 청산 모델14개 비교의 원본은 `logs/track-c/c3-exit-ablations-20260906.json`. 같은2만원에서 고정손절은4캠페인·−9.2원, 변동성+가치는3캠페인·−3.2원이었다. 매수대금이80,002.9원→20,604.6원으로 줄었고 평균 bp는 더 나빴으므로 수익성 향상으로 해석하지 않는다. 가치 모델은 이 짧은 표본에서 준비0·청산0이었다. 표본 요건을 낮추지 않는다.
- `c3_observations`는 거래 판단에 사용하지 않는 압력 에피소드·1Hz 프레임을 기록한다. 공격적 매도 한 건/직전 최우선 매수호가 금액≥1, 최신 공정가 대비 할인 조건으로 후보를 묶고32초 조용한 구간에서 닫는다. 이 꼬리표가 비정보성·일시적 충격을 식별했다는 뜻은 아니다. 거래 거부·미체결을 포함한다.
- `c3_live_evidence`는 원본 장부를 읽기 전용으로 열어 실제 체결·별도 미체결/진행 코호트, 관측 체결 시점 이후0/250ms/1/5/30초 수량별 매도 VWAP·비용 스트레스·취소/청산 지연을 계산한다. 실제 수수료·체결가에 이미 든 스프레드를 중복 차감하지 않는다. 외부가격과 프리미엄 변화는 별도로 분해한다. 잔량 이월은 완료 승리에서 제외하고 현재 소유 재고와 청산 가능 여부를 남긴다. 과거 데이터에 관측 프레임이 없으면 귀속값을 만들어 내지 않는다.

새 유동성 충격 설계는 연구 방향이다. 이번 코드가 보정된 queue-reactive·경쟁위험·체결 조건부 가치모형을 구현했거나 수익 우위를 입증했다고 주장하지 않는다. 실제 배포 시각과 건강 검증은 아래에 추가한다.

후속 교차 검토에서 정상 REST 대기 중 표본 공백이300초 위험 창을 초기화하는 문제를 재현했다. `RuleRunner.fair_sampling`을 같은 이벤트 루프에 추가하여 정상 조회 대기 동안 실제 수신 표본을 쌓으며, 과거 표본을 소급해 채우지 않는다. 감시견은 이 별도 표본 작업이 울리지 않는다. 실제 Coinone/leader 단절은 이력을 끊는다. 4초 조회 대기 및 단절 회귀 테스트를 추가한 전체208 테스트 통과. 첫 교체본은 PAUSE를 제거하지 않았고 주문 의도가0인 것을 확인한 뒤, 원본 장부 백업과 표시 기준선을 보존하는 `prepare --continue-paused`로 교체한다. 첫 교체본의 전향 평가·실거래는 시작하지 않았다.

입력 처리 계측: 고정 BTC34,786개 중34,780개 수락 식별자 해시가 전후 완전히 같았다. 3회 중앙값3.039초→0.580초(약5.24배). 늦은6건을 어느 계층에서 집계하는지만 달라졌다. 실제 새 HTTPS 연결 풀은 읽기 전용 조회5회에 연결1개, 실패0이었다. 서로 다른 API 조회의 RTT를 실주문 체결 지연 개선으로 과장하지 않는다.

## 배포 완료

2026-09-06T02:59:02.270+09:00에 release `20260906-024813-803e30a1e473`/PID22023로 재개했다. 변동성 이력 준비·공개/개인/두 외부 연결·watchdog20초·소스36/설정 일치·원본37,952행 보존·미재개 기간 주문 의도0 확인. 초기 미재개본을 수정하는 과정에서 서비스 교체는2회였으며 **거래 재개와 평가 등록은1회**다. 알림 `20260906-024021-6ff74fb7c709`는 매매 재시작 없이 갱신했다. EBS 원본8GiB 스냅샷 `snap-02fcfec9697315d68` 완료,32GiB 확장·여유 약28.5GB 확인. 영수증: `logs/track-c/c3-execution-final-health.json`, `c3-execution-peer-verification.json`, `c3-execution-resume.json`.
